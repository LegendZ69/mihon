# Translator controls, accounting and provider validation

Status: bounded implementation/validation completed; overall acceptance remains open, September 13, 2026 (Singapore). This continues the [SFX validation](translator-sfx-validation-2026-09-12.md), [geometry/fitting validation](translator-geometry-fit-validation-2026-09-08.md) and [quality/recovery validation](translator-quality-recovery-validation-2026-09-06.md). Native controls and geometry recovery have passing outcomes for 20 methods on both K90 and actual 16 KB, with the separate HyperOS launch restriction retained. Matched classic measurements, native-screen checks, cached exports and bounded sustained rendering measurements are recorded below. The actual 16 KB notification matrix passes on v13 after preserving the v12 failure. The selected warning review repairs its source-mask coverage; sideways Japanese remains unchanged after two timed-out attempts. Matched WebGPU measurement remains incomplete because its starting position is not repeatable. The v14 export-dialog contract passes on K90 and actual 16 KB; K90 posted progress and physical Pause with programmatic Resume also pass. Temporary settings are restored and the final owned recorder cleanup is verified. Human passage approval and overall acceptance remain open. Chronological evidence retains earlier failures and scope limitations; later results supersede only their explicitly matched checks.

## Preserved baseline

At the preserved baseline boundary, K90 main was the prior release SHA-256 `e7689d90dbe8c28f82f84dce58ec38f671f8568d4bb420a087a33d9b3c0ae49c`; benchmark was `38f62bf64508200d1935ee52d06fbc7d29a7f15c26a5dfe8746f5a31a9e9d0ad` at the baseline collection boundary. No new paid validation requests or cloud billing queries had been dispatched at this baseline boundary. The later bounded chapter retry and its spending reconciliation are recorded in the September 13 continuation below.

The fresh-process classic comparison is independently reconciled in `build/translator/validation/private/controls-baseline-reconciliation/`. Both builds used controlled chapter 002/page 7, one translated/original pair, device-timed 30-second gestures, the same viewport and a fresh process with no prior WebGPU reader use. Source page-start hash matches. Pixel comparison restricted to `[0,156,1200,2560]` finds **zero changed original pixels**; whole-screen differences lie in system/status/navigation bars and are preserved separately.

| Build | Mode | Frames | Frame duration p50 / p95 (ms) | Jank rows | App-deadline misses | Sampled GPU gauge range (MiB) |
|---|---|---:|---:|---:|---:|---:|
| Old `d245eb8e…` | Translated | 660 | 4.342 / 5.961 | 11 | 0 | 156.39–186.82 |
| Old | Original | 661 | 3.933 / 4.894 | 14 | 0 | 137.70–153.17 |
| Prior final `e7689d90…` | Translated | 661 | 4.302 / 5.550 | 17 | 1 | 151.40–195.46 |
| Prior final | Original | 661 | 3.917 / 5.240 | 10 | 0 | 144.30–159.77 |

Frame coverage is valid, but this is one pair per build, not a randomized or thermally identical experiment. Both were externally powered; battery temperature was 31.3°C on the old build and 32.4→32.2°C on the prior final. CPU counter coverage is incomplete across paired windows. GPU gauges are sparse observations, not measured peaks or allocation rates. No isolated rendering cause, smoothness guarantee or memory acceptance is established. This supplements rather than replaces the retained three-pair measurements.

The read-only post-inspector collection ZIP is `e70406f27556e7f70d372b900c39c326f504f4e84fd545a62441f1d6a4789252`. Independent verification in `private/controls-inspector-preservation/receipt.json` establishes **12 pages / 29 regions**, byte-identical result JSON and source PNGs, unchanged revisions and no added reviews versus the pre-reader archive. Raw OCR was unavailable in these controlled saved results; unchanged null fields do not prove OCR generation. No SQL writes, provider calls or Manager mutations were performed by the collector. Its baseline replay job remains deliberately retained for subsequent validation.

## Reproductions and implementation checks

All paths below are relative to `build/translator/validation/`. Overlapping test runs must not be added as unique coverage.

| Check | Current result | Evidence |
|---|---|---|
| Bound-key import through the JSON-only path | Formatting failure reproduced; typed authentication choice and bound-key request tests pass | `private/provider-controls-20260912/` |
| Groq Chat/Responses wire formats and text-only routing | Provider regression group passed 32 methods before later integration | `private/provider-controls-20260912/provider-final-green.xml`, `groq-final-green.xml` |
| Captures omit image bodies/signatures while API wire remains unchanged | Host regression passed | `private/capture-sanitization-20260912/` |
| Export of a legacy capture leaks retained sensitive payload | RED reproduced; sanitized export suite subsequently passed nine methods | `private/capture-sanitization-20260912/legacy-export-green.xml` |
| Settings autosave | Four public writer-boundary regressions pass (debounce, valid persistence, navigation/reset and storage error) | `private/controls-operations-20260912/autosaver-green.xml` |
| Durable operation/repository schema | Missing-table RED, then actual SQLite restart and migration preservation checks pass | `private/controls-operations-20260912/storage-red.log`, `scripts/tests/test_translation_operation_storage.py` |
| OCR operation reports its own stage and completion | RED, then one domain test GREEN | `private/controls-operations-20260912/operation-recorder-green.xml` |
| Chapter coverage from saved identities | Four domain assertions GREEN, including RED→GREEN for a two-page restored subset of a known five-page chapter | `private/controls-operations-20260912/coverage-red.xml` |
| White sampled background keeps automatic text visible | Actual minified Canvas RED followed by Canvas/GPU and review-evidence GREEN on the 16384-byte emulator | `emulator16k/controls/contrast-red/` |
| Inspector viewport clipping | Existing focused fix passes in that same minified invocation: zero changed pixels outside the viewport | `emulator16k/controls/contrast-red/` |
| Google bounded billing-query behavior | Initial connector checks GREEN; additional bounded-query, deletion-recovery and pricing safeguards are under verification | `private/accounting-20260912/` |

The contrast RED benchmark is `0605b309d41d55a0553791e07e586ae57eb81d4978ddd679ec9f6d78f9f757fd`; Android test APK is `c86f095ea94fb535763bad07721fdccbd896c07d66079c280ec5ab586c42dc1c`. The normal minified build and installed-hash verification are archived under `r8-instrumentation-abi/controls-contrast-red/` and `emulator16k/controls/contrast-red/installed.json`. The invocation ran two methods: one intentional contrast failure and one viewport pass. The failed contrast assertion occurred before its GPU assertions, so this run does not establish GPU contrast behavior.

Aggregate `:domain:test --tests ...` was rejected before testing; the corrected task is `:domain:testDebugUnitTest`. A later capture attempt failed at app compilation due to an unresolved request-ID property; no behavior was tested in that attempt. Both setup failures are retained rather than labelled RED behavior.

## Intermediate minified verification on the 16 KB runtime

The next normal minified benchmark build is SHA-256 `21eef8c3d2948db7cc63b1ae4c9004890963aaad72f408b05ac23b29f528d0c8`; its Android test APK is `8c6feac103029e552ce1c3db77d1c6a990482cd4ae6307fcbcdac511c5594b46`. Both installed APKs were pulled and independently hashed. Source files were frozen through application and instrumentation compilation; the source-manifest SHA-256 is `8d12054ee20d11c6323f040c0f25e18c458d44a575afa35bcbadbd3750569394`. The build archive is `r8-instrumentation-abi/controls-contrast-green-archive-red-v2/`. This is an intermediate build, predating subsequent management and notification changes.

The actual `16384` runtime invocation ran **22 methods: 21 passed and one intentional archive-repository RED failed**. It passed sampled automatic/custom contrast, actual Canvas/GPU rendering, fallback diagnostics, inspector viewport clipping, 14 rendered-review evidence methods, two theme checks, and real SQLite operation/usage restart preservation. The viewport test observed zero changed pixels outside the viewport and 1,152,000 source pixels inside. The sampled contrast evidence preserved result revision 42 and pinned its resolved foreground across theme changes. All 14 review-evidence fixtures reported zero provider dispatches and owned-directory cleanup. The interruption fixture cancels before preparation; it does not demonstrate interruption during image decoding.

The sole RED was the newly introduced backup persistence stub returning zero imported pages instead of one. It did not reach its restore/Undo assertions; those remain pending until rerun against the implementation. The independent reconciliation, exact command, installed hashes, complete log and scope limitations are in `emulator16k/controls/contrast-green-archive-red-v2/independent-reconciliation.json`. A zero shell exit code did not turn this JUnit failure into a pass.

Subsequent host checks pass the four coverage tests, two operation-recorder tests, four usage-lifecycle checks, existing Manager/Paddle regressions, and initial billing/backup/capture suites. The new HTTP-error echo test passed on its first run; no new defect or fix is claimed for it. The actual SQLite page-log query did reproduce a separate failure (only the page event appeared), then passed after associating its shared batch and child review request events and enforcing time bounds. Evidence: `private/controls-operations-20260912/page-log-correlation-{red,green}.log`. Integrated generated-query and runtime coverage is still required after these changes.

## Transfer and notification runtime checkpoint

The subsequent intermediate minified benchmark/test hashes are `99cdf510704957bc6ca01d85bd7e0b1db7cedd9302f1411c63e010c21e863a2e` and `356dd123e946d2da81620375816329541796166b8b2cb176cdf565d58c8c9c86`. Both were installed and independently pulled/hashed on the actual `16384` runtime. The source freeze, normal R8 build, manifests, certificates and native alignment are archived at `r8-instrumentation-abi/controls-transfer-notifications-v2/`; a preceding nullable-CharSequence test compilation failure is retained separately as setup evidence.

Seven methods ran: **five passed, two failed**. Default archive restore now passed its previously failing real SQLite test, including preserved raw OCR/revision, restart, duplicate import and repair Undo. Notification objects passed shared chapter/download card limits, distinct region/byte/page progress and scoped action labels. Page logs passed shared-batch and review-child request linkage with time bounds. Translation-only deletion preserved original files, accounting and other jobs after restart, and a concurrent edit prevented deletion of the selected set.

The deliberately missing Android export raster adapter produced the expected incomplete-export RED before its pixel assertions. The other failure compared two review timestamps differing by four milliseconds. Inspection of the archived source identified a fixture issue: default JSON encoding could omit the dynamically defaulted timestamp, and each subsequent decode supplied the current time. The fixture now encodes defaults with a fixed timestamp; the complete equality assertion remains and a separate real Undo assertion has been added. The rerun is pending; no production deletion change was made for this failure. Notification objects were inspected without publishing notifications or changing permissions; actual UI actions remain pending. Evidence and an independent scope-aware reconciliation are at `emulator16k/controls/transfer-notifications-v2/`.

## Management and dashboard runtime checkpoint

Normal minified build `controls-management-red-v4` is archived with benchmark SHA-256 `f9f47de1584a42637ba75e94b0462c750ef7963334992a84131d7d05f22f4d70` and matching test `679061f818f74c422f63ef45c5206adf4677e9cf49d50e5ee5fea5f8b77f7bb1`. Source freeze covered 1,114 files through both compilation boundaries; both installed files were pulled and hashed on the actual **16384-byte** emulator. The preceding v3 failed test compilation, so it provides no runtime result.

The run reported **17 methods: 11 passed, five failed, one opt-in method skipped**. Four failures were deliberate missing implementations: explicit archive replacement, archive relinking, historical log import and atomic job replacement. The fifth was a fixture assertion incorrectly expecting a blank credential reference instead of the application's nonsecret `default` reference. The preference writes/debounce/invalid-draft checks reached that final assertion; no production autosave change is justified by it. Separate-process persistence was not invoked. The compact-review-summary test was excluded because its archived fixture lacked parent job rows; there is no passing or failing runtime result for that method yet.

Positive results include default archive restore and all-or-nothing rollback; the shared Android Canvas CBZ renderer across a **30,000-source-row** strip, including the band boundary, final source rows, preserved SFX/artwork and unchanged original bytes; all three scoped deletion checks, including retained review history and actual Undo; two actual SQLite dashboard/billing tests with 1,008 request records, exact token/cost aggregation, restart deduplication and scoped attribution; one autosave scope/reset test; notification presentation objects; and page-log correlation. Published notification actions and cloud billing access remain separate gates. The timestamp fixture failure from the preceding build is now resolved without changing production deletion.

Evidence: `emulator16k/controls/management-red-v4/independent-reconciliation.json`, complete instrumentation output, installed hashes and `r8-instrumentation-abi/controls-management-red-v4/`. No cloud requests or billing queries were dispatched.

Current K90 preflight independently reverified main APK `e7689d90…` and framework harness `67e851d5…`. UI evidence shows automatic translation **off**, automatic review **off**, chapter-ahead **0**, and **Ignore sound effects on** for the controlled series. The earlier restoration note claiming this override was still off is stale; no SFX toggle was made in this preflight. New paired reader measurements use the same on policy on both builds; earlier off-policy measurements remain historical. The read-only framework inspection confirmed corpus chapter 002, page 7. The first host calibration launch failed before ADB execution because the executable was absent from PATH; the corrected invocation supplies its absolute path and preserves the setup failure separately.

Published notification actions subsequently passed on the same v4 benchmark and actual 16384-byte runtime. The Pause→Resume run dispatched three local requests and finished two saved pages; Pause→Cancel dispatched two requests and retained one saved page. Independent ledger matching proves the **first committed page (image ID 1 in both runs)** was unchanged and dispatched once. This does not establish source-ordinal-zero ordering. Both tests invoked PendingIntents obtained from actual posted chapter cards under active instrumentation; they do not represent a finger tap, natural idle or HyperOS eviction. Benchmark notification permission and flags were restored to their original off state; each owned server and reverse mapping was removed. Test credentials and jobs were removed, while private fixture files and synthetic telemetry await verified collection. Evidence is under `emulator16k/controls/notification-{actions,cancel}-v4/`, with independent reconciliation receipts. Delayed canceled server requests retain their dispatch records without inventing terminal HTTP outcomes.

The current-build classic calibration passed its two measured frame windows and six screenshot checks. The following three alternating pairs passed canonical content-ROI identity independently across all **14 PNGs** and **3,964 frames**. The ROI `[0,337][1200,2240]` excludes status/toolbars; same-mode before/after and repeated starts are identical, while original and translated images differ. All six frame windows have complete expected-deadline attribution; CPU coverage is incomplete for windows 3, 4 and 6, so only pair 1 supports a complete paired CPU comparison. Translated p95 frame durations were **5.471 / 5.687 / 5.413 ms**; their paired originals were **4.924 / 5.219 / 5.147 ms**. This is the baseline on unchanged APK `e7689d90…`, PID 31664, with Ignore SFX on and external power; final-build comparisons remain pending. No performance pass is inferred from the absent App Deadline Missed labels. GPU timing and allocation rate remain unavailable. Evidence: `private/controls-current-classic-{calibration-v2,three-pairs}/`, including raw traces, complete controller journals and `independent-baseline-reconciliation.json`.

To preserve capacity for later builds, 36 byte-identical archived APK copies were replaced with independent APFS copy-on-write clones of their matching archived contents. All paths, bytes, hashes, modes, extended attributes and modification times were preserved and verified; no evidence was removed. The filesystem reported **571,592,704 bytes** of recovered space at that checkpoint. `private/controls-artifact-deduplication/` records the duplicate inventory and each before/after hash. This is a storage representation change, not a new APK or a rewritten validation artifact.

## Archive navigation and query consistency checkpoint

The public service tests reproduced and then fixed three additional gaps: re-backing up an unlinked archive could replace its saved presentation with global settings; committed restores lacked a target-correlated receipt; and an already-imported unlinked archive had no safe relink operation. The original archive is retained, final source revisions/history/styles are checked after original acquisition, and cancellation messages distinguish already-committed results. Structured backup lineage no longer restores a live replacement pointer. Two chapter-selection/grouping regressions also reproduced active-work selection and unrelated unlinked histories being grouped together. The affected **15 host methods pass** in `private/management-20260912/host-results-v6.json`; codec, service, chapter selection and grouping scopes are recorded separately.

Management now retains its immutable export request through the document picker using saved instance state. Rendered export, relink, archived-style application and deletion remain explicit actions. Dashboard request and save charts open paginated operation records using the chart's update-time interval; save charts additionally require completed operations. The native filtered-operation and compact-review-summary query assertions are intentionally awaiting their first actual SQLite execution. Global `spotlessApply` passed at `private/controls-final-checks-20260912/format-v3/`; source formatting uses no lint suppressions.

Fresh read-only pricing evidence is preserved under `private/pricing-fx-20260912/`. The [Google pricing table](https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing) describes the current promotion using billing credits, so live-validation holds use the conservative pre-credit rates without assumed cache hits. The [ECB reference XML](https://www.ecb.europa.eu/stats/eurofxref/eurofxref-daily.xml) dated September 11 gives EUR/USD 1.1592 and EUR/SGD 1.4697, or exactly 71/56 SGD per USD. Full-context and maximum-output capacity over two attempts would reserve **S$5.24 rounded upward**, before any separately applicable account currency/tax/tool differences. This is a maximum reservation calculation, not a measured request cost or invoice. No ledger settlement, cloud query or paid dispatch occurred during the refresh.

## Integrated minified transfer and persistence results

The full translator host invocation passed **256 methods across 34 suites**, with zero failures/errors/skips. Archived XML hashes and counts were independently rechecked at `private/controls-final-checks-20260912/integrated-host-v5b/`. Global formatting and debug/release SQLDelight migration verification passed. An initial combined command could not select the debug app unit task under the benchmark configuration, and a separate host command initially lacked its SDK environment; neither reached behavior testing. Corrected invocations preserve both setup failures separately.

Normal minified v5b benchmark SHA-256 is `4de5eb5ec9d4c6ac19fb05f00ceb2245cfb3df3c5ea0f335b5d2c167fb29899b`; test APK is `b89c13606d31411c0e7ab317c326aee557088c63e8e360c3e7b67c6748900135`. Both installed files were independently pulled/hashed on the actual **16384-byte** runtime. The source freeze and normal R8 archive are `r8-instrumentation-abi/controls-integrated-v5b/`.

The 15-method device invocation yielded **13 passes and two intentional query REDs**. All five archive persistence methods passed, including explicit replacement, content-identity relinking, historical-log deduplication and rollback. Both A4/Letter Android PdfRenderer checks passed. Atomic replacement now preserved saved revisions/history and deduplicated creation across restart. Corrected autosave methods and compact coverage of known totals, blank/SFX-only pages and missing totals passed. The two failing methods exposed the intentionally missing compact review summaries and ignored operation filters; their fixes are pending runtime rerun. Evidence: `emulator16k/controls/integrated-v5b/independent-reconciliation.json` and complete output. These fixture results do not establish final handset, live-provider or human visual acceptance.

The two earlier notification fixtures were collected and independently verified, then cleaned using matching immutable ZIP digests. Retained ZIP hashes are `2442ee2a3f51c89f053d38a16b1cf3911d1bcbdab499109b5f71d75ea255ff19` and `b3abb3edb5df6527091c28b7549a8e8661aa31987268870146538d3e02dba181`. They contain 10/6 operations, 29/17 events and 3/2 usage records respectively. Cleanup removed only the verified fixture roots and exact synthetic telemetry; the host archives, shared queue history and unrelated records remain. Complete commands, independent receipts and cleanup logs are at `emulator16k/controls/notification-evidence-v5/`.

The opt-in committed-settings check passed in writer PID 9081 and reader PID 9140, preserving isolated global and series preferences and deleting only its fixture preference store. The writer had already exited before the explicit force-stop command; `stopped=true` was observed before instrumentation relaunched the target. This establishes committed-setting persistence across separate processes, not killing an active writer, UI draft restoration or natural eviction. Its bounded scope and checkpoint markers are preserved in `emulator16k/controls/autosave-process-v5b/`. No paid request or cloud billing query occurred.

## Final candidate and cancellation verification

The v6 normal minified release, benchmark and test artifacts are archived at `r8-instrumentation-abi/controls-final-candidate-v6/`. Their hashes are respectively `361635da64838015e721a793b25e7cafa61c8d6326e93946681a8cf22bec5f0b`, `cdb57af0de06ecfa1ccfc0ae584a3c47ccdcd9876a05f33d005f22e53e0aaec9` and `6d49240e22f44841513d98ffcdfb278b05d293343e69fb6b6ac7063fa9ebcd31`. All 1,122 source files remained identical through completion of all three builds and archive verification. Formatting and migration verification passed.

On the actual **16384-byte** runtime, the v6 dashboard persistence invocation passed **all five methods**, resolving both v5b query REDs. The independent receipt rehashed the archived APKs and all source entries, verified installed receipts and parsed complete instrumentation outcomes. Compact review summaries preserve latest review identities without loading baseline/OCR/evidence payloads; filtered operations honor scope, time bounds and stable pagination. Evidence: `emulator16k/controls/query-green-v6/independent-reconciliation.json`. Review-only control updates also have a reproduced host failure followed by eight passing related methods in `private/accounting-20260912/controls-review-green/`.

A subsequent management review reproduced three cancellation/admission failures. The corrected operation gate stays occupied through cancellation cleanup, assigns its job before an immediate completion can clear it, and only clears the matching job. Restore and relink dialogs expose an in-modal Cancel operation; staging cleanup waits for termination, including navigation. The six focused gate/service methods pass after the three REDs, and formatting passes. Native modal acceptance remains pending. Evidence: `private/management-cancellation-20260912/`. These source changes supersede v6 as the final build candidate; v7 is being built normally.

For storage headroom, nine owned mapping files were transparently compressed using `ditto --noclone --hfsCompression`. Paths, complete logical bytes, SHA-256 hashes, permissions, owner and modification times are unchanged; APFS compression flags and physical representation changed explicitly. Independent verification checked every current file. This retained all evidence rather than deleting or renaming mappings. The compression receipt is `private/controls-storage-compression/compression-receipt.json`.

The earlier current-build reader pairs did not retain explicit refresh-policy settings. A fresh calibration and paired baseline are being collected on the still-installed old main APK with read-only refresh/display/brightness/power observations outside measured windows. Earlier pairs remain valid within their recorded scope; the new observations do not retroactively prove their refresh policy.

## K90 reader crash and preserved results

Normal v7 artifacts built with formatting/migration verification passing: main `bcde60ad27c4c59fdc596585936111dcc4511136b3189e7c263ace1e74414763`, benchmark `774d87aa2984494e9695c14be19c4353cc3b4f31dc416f5c379acc2bf477dbd7`, test `6d49240e22f44841513d98ffcdfb278b05d293343e69fb6b6ac7063fa9ebcd31`. The actual 16384-byte emulator passed three focused methods for sampled Canvas/GPU contrast, pinned review contrast and inspector clipping; installed APKs and archived sources were independently verified. Evidence: `emulator16k/controls/final-focused-v7/`. This did not exercise the reader's PNG corner-decoding path.

The old main was independently pulled/hashed before replacement, and the installed v7 main was pulled/hashed afterward. Opening controlled chapter 002 from History then reproduced **two native K90 crashes** at 11:40:55 and 11:41:56 Singapore time. The first ended in RecordingCanvas/libhwui; the second reported a Scudo corrupted chunk header during PNG rewind in `BitmapRegionDecoder.decodeRegion`, called by `sampleTranslationBackgrounds`. Process exit records confirm separate crashed PIDs and the prior package-update termination. Do not infer process lifetime from the unusual native tombstone uptime alone. Logs, UI sequence and exit records are under `k90/controls/main-installed-v7/`; the scoped diagnosis is `private/controls-sampling-native-crash/`. The new translucent-mask contrast path invokes this sampler where the earlier controlled settings did not. A Java stream-lifetime misuse is not established: AOSP copies the encoded input into owned storage and its filename overload itself closes the stream.

Reader timing/export work using that sampler is paused until a focused captured-PNG regression and bounded decoding fix are validated. The failure remains a required acceptance gap; unrelated passing Canvas tests do not clear it. A separate UI review also reproduced and fixed verified-text-only Groq controls still being visible and the zero-image limit being labelled unverified. Two focused presentation tests and formatting pass; their source changes will enter the next normal build.

The actual v7 Management screen exported exactly the selected controlled history through the Android document picker, with logs and sanitized payload options. Independent verification of `k90/controls/management-ui-v7/baseline.zip` proves all **12 pages / 29 regions / 12 revisions** match the retained baseline after documented Kotlin-default materialization. No existing result field changed or disappeared; 251 previously omitted defaults are now explicit. Original identities/hash/dimensions/order match. Raw OCR, corrections, review checkpoints and API captures are absent in both copies, so this proves preserved absence, not nonempty-history coverage. The two log attachments contain only the selected job's event/operation. The export completed without provider calls and preserves the two live-review target baselines.

## Archive stream reproduction and native OCR stages

The first captured-PNG sampler regression passed on K90 v8: one method, **36 samplings** across the twelve synthetic pages. That test used asset-byte streams, bypassing the actual local CBZ stream, so it did not reproduce or resolve either reader crash. Its `sampling-red-v8` directory name records the intended experiment, not an observed failure. The exact owned chapter CBZ was then collected, SHA-256 `8c5510c274704a2760ad877df9f3354638fa0665fd11e79e76f722085088b459`; all twelve members match the PNG assets, dimensions, hashes and saved revisions independently. Evidence: `private/controls-sampling-native-crash/fixture-and-red-reconciliation.json`.

Normal v9b benchmark/test artifacts are `12d661868025203d528d7e57899101446866e71aebadbee9d662964eb2d75035` and `edbc359496a69192b9c6cfd45f31ba7871919660fbddf9c2b030548c6ef22dc0`, archived with the additional `core/archive` source freeze. Both were installed and independently pulled/hashed on K90. **Both native contract assertions failed**: a request for three bytes returned 32; a request for zero returned eight. The archive adapter cleared a wrapped buffer back to its whole-array capacity, losing the requested offset/length. The focused fix slices the requested range before clearing and returns zero for a valid empty read, retaining range validation. This follows the documented [InputStream contract](https://developer.android.com/reference/java/io/InputStream) and [ByteBuffer slice bounds](https://developer.android.com/reference/java/nio/ByteBuffer#slice()). It does not change translation results or the overlay algorithm. Runtime GREEN and the actual reader relaunch remain pending. Evidence: `k90/controls/archive-stream-red-v9b/` and `r8-instrumentation-abi/controls-archive-stream-red-v9b/`.

The K90 v9b local OCR operation test separately passed using the existing official Tiny pack, with downloads disabled. The persisted report records 141 ms total including 43 ms cold model loading, 58 ms detection and 8 ms recognition on the 960×320 controlled English fixture. One region retains measured detection/recognition scores and exact raw OCR; operation IDs connect preprocessing, detection and recognition to their events and measured durations. This uses a repository observer rather than writing production operation rows. It is one bounded fixture, not a profile comparison or latency guarantee. No provider requests or model downloads occurred. Independent reconciliation verifies five distinct operations, ten correlated start/finish events, exact raw/typed geometry and scores, both installed APK hashes and all 1,158 archived source entries. Nested timings overlap their parent stages; the unallocated 25 ms remainder is not separately attributed. Evidence: `k90/controls/measured-ocr-stages-v9b/independent-reconciliation.json` and `collected/report.json`, SHA-256 `58f85c28063da84e04b94e6014046e3858adc54478d5717c264eac94dca02f18`.

## K90 management round trip and cancellation

The exact disposable unlinked job `validation-management-cancel-9fd4f51a-b3ea-4f3f-88c6-1c9ecc1bb515` was restored and re-exported through the v7 Management UI. Independent comparison passes **500 pages, 8,000 translated regions, 8,000 raw OCR regions, 500 transcription corrections**, saved revisions and effective styles. The synthetic source has no originals, credentials or pending work. Review history is absent, so this UI run does not replace the earlier nonempty native review/Undo checks. Evidence: `k90/controls/management-ui-v7/owned-roundtrip-independent.json`.

Scoped deletion preview showed exactly the selected disposable history, 500 saved translation records and its logs. Deletion left the owned history paused at **0/500 saved pages**, preserving image identities and accounting by design; the job row was not deleted. No source chapter, unrelated translation or credential was selected. A second restore was cancelled through the actual in-modal action. The pinned, permissionless UI helper accepted both Restore and Cancel and observed modal disappearance at 220 ms (75 ms after Cancel), then verified the target APK hash. The intermediate Cancelling state was too brief to observe; private staging removal was not inspected. Subsequent UI confirms the owned job still has zero saved pages. This establishes accepted cancellation and no committed restored pages, not visible-cleanup-state or private-file acceptance. The screen exposed a raw coroutine cancellation message; a focused wording correction now preserves committed-restore warnings while replacing internal coroutine text. Its runtime presentation awaits the rebuilt APK. Evidence: `management-ui-v7/helper-owned500-v7-002.jsonl` (SHA-256 `b35a1fbe39cab287437ac9ba55c8985aa788f7e0e597f9bd5e70823f5c0e630d`) and `current-reader-preflight/main-v7-owned-after-cancel-v2/`.

The earlier helper v1 accepted Restore but stopped before Cancel when its strict window/bounds check changed; that import completed and is the round trip above. v2 preserved the exact window ID while accepting bounded same-window layout changes. Neither run is retroactively upgraded by the other. The main v7 package and saved controlled-page baseline remained unchanged. After collection, three created transfer ZIPs were rehashed against retained host copies and removed; the owned permissionless UI helper was uninstalled. The paused zero-page history and deletion receipt remain by design. Evidence: `management-ui-v7/owned-transfer-cleanup/`.

## Final settings/accounting regression checkpoint

Three additional focused host failures were reproduced: settings export retained synthetic private values in URL/advanced payloads; a Groq price opened the Google pricing link; and a missing-usage response lost the source/date of its uncertain reservation. The corrected batch passes **8/8 methods** across settings export, settings presentation and request-usage lifecycle. Nested advanced payloads now use the bounded shared sanitizer before any settings-document write; a failed or incomplete sanitization aborts export. Configured language, glossary, automatic translation and chapter-ahead values remain settings exports, while translation ZIP portability retains its separate inactive-work policy. Uncertain requests retain their reservation amount/currency and price provenance; this does not settle their cost or release their hold. Known-usage settlement and actual provider requests are unchanged. Source/date preservation is tested; nonnull FX preservation has not received a separate fixture. Evidence: `private/controls-ui-checklist-20260912/followup-{red,green}/`, including independent eight-method reconciliation. Formatting passed; the subsequent v10 normal builds and focused runtime checks are recorded below.

K90 read-only UI preflight shows global automatic translation off, automatic review off and chapter-ahead **5**. The controlled-series override remains the separate chapter-ahead **0** measurement policy; global defaults were not changed during this preflight. Benchmark Worker preflight shows zero jobs, no unfinished work, online connectivity and notifications disabled. No paid operation was dispatched.

For build/trace headroom, 41 previously uncompressed owned `.pftrace` files were transparently compressed. Paths, full logical bytes, hashes, permissions, owners and modification times were rechecked; only physical storage/compression metadata changed. No trace was removed or renamed. The receipt `private/controls-historical-trace-compression-v10/compression-receipt.json` records each file and approximately 3.54 GB available after this operation; available space is a point-in-time filesystem observation.

## Integration and remaining gates

The new operation, usage and billing tables use migration 17. Existing JSON snapshots retain compatibility; no old result/raw OCR/review payload rewrite is planned. Request attempts persist before dispatch, including uncertain reservations; usage settlement is separate from content acceptance. Translation deletion must retain accounting unless accounting is explicitly selected for deletion.

Global/per-series preference edits do not dispatch providers. Credential import remains explicit. The Groq configuration action creates a new editable Paddle/text-only setting; it does not mutate incompatible queued snapshots. Translator Wi-Fi gating is removed independently of Mihon's source-download restrictions.

New and exported API captures are sanitized diagnostics, not exact replay archives. Historical external validation archives remain unchanged. Automatic contrast resolves against sampled/composited backgrounds through the shared renderer; explicit foreground choices remain explicit and receive diagnostics. Cached presentations can change without saved result revisions changing.

The archive-stream fix, v10 installed hashes and seven affected methods on both runtimes subsequently passed as recorded below. Remaining checks include the new native-controls/recovery final builds and runtime paths, handset notification/actions and local chapter filtering, management UI rendered exports, reader/pacing/memory measurements, two unresolved live controlled geometry reviews and restoration/owned-fixture cleanup. The v7 normal builds and hash verification, structured backup round trip and scoped deletion have completed within their recorded scopes. Completed host, migration, query, transfer and emulator notification checks are recorded above; they do not substitute for the remaining handset checks. The cumulative S$300 ceiling/S$270 dispatch stop and uncertain holds remain mandatory; refresh dated pricing/FX and reserve each maximum before any paid operation.

## v10 archive-stream and native-controls continuation

The normal v10 artifacts completed after the recorded fixes. Main APK SHA-256 is `55588d60b8fa33d6aa3d02643fd96cb7c0e4ca66138fdf95a7a8ababc1965826`; benchmark is `ea6f6189d4d04a9de5075150bd506b604f0b4ffbabaaa41f8cedc64d27e443ed`; instrumentation is `259e4c1b2548f2794fced8e975a314b26573dc34d3b2352389f55e6667308acd`. The retained `controls-final-v10` source freeze covers 1,160 files. Both K90 and the actual 16 KB runtime passed seven focused methods: two archive-stream contracts, two repeated sampler paths, sampled overlay contrast, pinned review contrast and inspector viewport clipping. The two sampler paths each record 36 complete samplings per device. Controlled chapter 002/page 7 subsequently opened in the main app with the same before/after process ID and unchanged crash buffer. The earlier two native crashes remain failures of v7; this narrower v10 result does not complete extended pacing or memory acceptance.

At the next execution start the K90 main APK was pulled again and its complete hash matched v10. Read-only inspection found `カップルゲーム【マンガ版】 14-3` paused with 20/21 saved pages. The first batch-scoped log view was empty; subsequent job-wide export recovered 260 events. Both failed batches identify original image `7` and tile `7~0_0_1000_1409`, origin 0,0, source/upload 1000×1409. Thus this particular failure concerns visible page 8, established from recorded input identities rather than the “images 8” label. The latest failed capture was opened explicitly and reported unavailable or removed by retention; the exact rejected region and coordinates remain unavailable. No request was dispatched during this inspection. Private evidence: `private/native-polygon-20260912/{device-baseline,joblogs-reconciliation-20260913.json}` and `k90/controls/native-polygon-v10/`. The official emulator was subsequently restarted and independently reported `16384` bytes.

New work follows the approved native Mihon controls and geometry recovery plan: shared native swipe/selection components, lossless redundant-vertex normalization, and one durable targeted correction pass. Synthetic domain regressions demonstrate the old closing-ring and duplicate/collinear rejection independently of the missing real response. These are implementation checks, not a claim that the specific missing real polygon has been diagnosed or repaired. Overall acceptance and human passage review remain open.


## Native recovery implementation and bounded live retry — 2026-09-13 SGT

Native queue/log/management/model-pack controls now use the shared Mihon swipe component, contextual menus, selection bars, filter sheets and app bars. Start/end actions remain configurable and logical-direction aware. Long-press selection uses stable identities, disables swipes, and clears through Back/up first. Existing chapter swipe defaults and persisted names remain; translation choices are additive. Dashboard and inspector navigation now use native menus/page selection without changing reader scrolling, zoom, polygon dragging, or explicit inspector Save/Discard. Host compilation and standalone formatting passed at their recorded checkpoints; actual gesture/TalkBack and final minified validation remain pending. Four isolated Android gesture methods cover threshold/RTL, selection/update/reordering/Back, accessibility and disabled actions.

Geometry boundaries preserve perimeter order while removing only redundant closing, consecutive duplicate and strictly between collinear vertices. Typed failures still reject crossings, concavity, backtracking, invalid bounds, nonfinite coordinates and zero area. The domain/coordinator batch independently passes 38 methods; the provider wire passes 19 methods, including five preserved RED→GREEN cases for malformed numeric geometry and pure Paddle rotation. Targeted correction preserves region/page identities, text, unaffected regions and raw OCR. Checkpoint identity and attempt reservation are persisted before HTTP; content failures terminate the pass, transient failures consume at most two attempts, and authentication pauses require explicit Resume. Log deletion retains this recovery state; translation deletion clears its saved copies and invalidates its policy atomically. No SQL schema migration was added. Five SQLite checkpoint/deletion methods await final runtime execution.

The integrated Gateway host batch passes seven methods covering one-image correction, partial success, malformed repair, bounded transport, authentication Resume, legacy snapshots and image-free pure Paddle. Two additional Manager regressions exposed duplicate retry changing the pass while WAITING and ordinary retry changing completed work; their focused guard fix subsequently passed all 41 Manager methods, preserving the earlier failures in `manager-red-wire-green/` and the GREEN XML in `prepared-sharp-red/`. Archive optional-history restore reproduced a decoding failure after checkpoint sanitization, then passed: portable history keeps operation facts while dropping executable correction state before decode and at the repository boundary.

A real PNG/SQLite prepared-Gateway test on the actual 16384-byte runtime reproduced a separate failure: one original page completed instead of two. Investigation found that prepared images carried a recipe/cache hash while immutable correction input validation expected the encoded PNG byte hash. This is a new recovery integration defect, not evidence about the expired historical model polygon. The sharpened byte-hash assertion then failed against recipe identity. After the focused fix, the actual 16384-byte debug runtime passed encoded-byte identity, prepared correction and transform/raw-OCR preservation, legacy-cache reuse and mismatch rejection, authentication restart/Resume, and cancellation: six of seven prepared-pipeline methods passed. The remaining method separately reproduced a terminal authentication error being hidden by an earlier content failure. Final normal minified reruns remain required. The debug reproduction is pinned to APK `94b15236f831cb5f78afe6fb8ba6dc5e8881daec407f1c3734b410e3a7588dfb` and instrumentation `e1fe588c06cb3a44346b477b399e4ea344ae28b880b504d5dc22ca7ce2e9797a`; it does not replace required normal minified testing.

One explicitly bounded **Retry failed** action ran against the installed v10 target job `8768aad1-80c0-4ac4-bf91-6eee8c55ba6a`. Its immutable historical settings retained Vertex service-account/OAuth, `gemini-3.8-flash`, `global`, shared **Priority** PayGo, HIGH thinking, five initial transport attempts, no automatic review and Ignore SFX false. These are historical job choices; the application defaults and current effective Ignore SFX presentation remain unchanged. A private reservation covered all five possible attempts before the single tap.

The observed chain was Count Tokens HTTP 200 → generation HTTP 429 → generation HTTP 200. Only image `7` was submitted and saved. All **20 previous result objects and revisions are identical**, and review history is unchanged; the chapter now has **21/21 saved pages**. The fresh result has three valid rectangular regions (`polygon:null`), so it did **not** reproduce the old polygon rejection or exercise the new correction code. The SFX region remains inspectable and excluded by the current presentation policy. No repeated paid call was made to obtain a polygon failure or a passing repair. Passage meaning, physical placement and artwork coverage remain separate visual/human checks.

All three fresh sanitized captures are complete; independent inspection found credential/image/thought-signature omissions and no unsafe retained payload. Both generation requests have the same source-body hash. Count/generation input dimensions and mapping agree. Usage was 2,095 input tokens (1,092 image), 435 visible output and 5,159 reasoning tokens. The official [Google price table](https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing) and [ECB reference rates](https://www.ecb.europa.eu/stats/eurofxref/eurofxref-daily.xml) were refreshed before dispatch (ECB observation 2026-09-11). The conservative gross Priority reservation was **S$23.556097**; reconciled usage is **S$0.102919**, independent of billed amounts. The cumulative ledger now records estimated usage **S$1.131267**, retained uncertain holds **S$78.461513**, and exposure **S$79.592780**, leaving **S$190.407220** before the S$270 dispatch stop. Prior request records and both uncertain holds are unchanged. No billing observation was obtained.

Evidence is under `private/native-polygon-20260912/`: `geometry-wire-green/`, `domain-checkpoint-green-v2/`, `manager-red-wire-green/`, `prepared-debug-red/`, and `live-v10-bounded-retry/`. The latter includes reproducible reconciliation, before/after ZIPs, request/capture hashes, usage, settlement and cleanup receipts. Three newly exported device files and the verified earlier baseline ZIP were removed only after matching retained host hashes. Credentials, production source chapters, saved results and private historical evidence were preserved. Final builds, new device paths, reader measurements, controlled visual reviews and remaining settings/fixture restoration keep overall acceptance open; AI output is not human passage approval.


## Recovery audit and native selection regressions

The independent audit batch reproduced **four Gateway failures**: missing prior-result revisions in correction checkpoints (including an edit during generation), a retained authentication pause allowing a later generation, and the same pause allowing Count Tokens. Revision snapshots now precede preparation and HTTP; the original-image revision guards every new checkpoint. Persisted pauses are checked by job and recovery epoch before preflight/generation, including sibling physical groups. A new epoch remains independently retryable. The corrected Gateway batch passes **12 methods**, including the historical-pause guard. A separate actual 16384-byte prepared-pipeline regression reproduced `AUTHENTICATION` being returned as `CONTENT`; terminal failures now take precedence while preserving accepted tiles.

The audit also reproduced **two Manager Cancel failures**: a mixed selection relabelled a completed chapter, and cancelling a completed chapter’s explicit region request relabelled its history. The focused guard preserves the completed state while still cancelling its explicit in-flight request. These new tests await their GREEN run. The subprocess selection audit found siblings sharing the chapter identity; operation-identity selection and diagnostics-scoped actions are being corrected before final UI acceptance.

All **72 translator domain methods in 11 suites** pass, with no failures/errors/skips, and SQLDelight migration verification passes. Evidence is under `private/native-polygon-20260912/{recovery-audit-red,cancel-red-gateway-green,domain-final}/`. Independent receipts exclude two missing-SDK setup failures from behavior counts. The prepared run’s archived and pulled-installed debug APKs match `8cabd301ee57ce6dde068302cee776412f2e6dade4cc6767c16854ad1aa1e4c5` and test `1fe5c306c54b3d42581bc32bd61cba3d1226ecbc7dfe94290b96bd2361b1b63b`.

The first four gesture methods were obstructed by an emulator System UI ANR dialog, independently identified as the input-owning window. After dismissing that system dialog, long-press/selection/reordering/Back and disabled-action checks passed; accessibility and completed-swipe checks still failed and remain under investigation. These failures are preserved separately from recovery counts and are not relabelled as successful native acceptance. No cloud request was made by these synthetic tests.


The full app translator/manga host invocation ran **262 methods in 35 suites**, initially passing 260 with the two deliberately retained subprocess identity failures. After replacing the three chapter-key references with operation IDs, all four subprocess tests pass; the other 258 methods are unchanged. The reconciled final outcomes are **262 passing methods**, including all 43 Manager and 12 Gateway tests. The earlier failures remain archived; `host-final-reconciliation.json` identifies the exact superseding suite. Selected subprocess exports retain operation snapshots and correlated, paginated events through a frozen time boundary, and optionally sanitized captures. Chapter-wide log deletion remains explicitly previewed and does not expose translation cancellation/retry as an operation-selection action.

The corrected real prepared pipeline subsequently passes **all seven methods** on the actual 16384-byte debug runtime, resolving authentication precedence and preserving accepted pages across pause/Resume, cancellation and legacy-cache reuse. APKs are `d9435cb63edb4fe0f690c8896b2b4b71e51dc0cfaa17a9c5b5099b0377405bcc` and test `3e285def84197e1435f1741a8d5d7cfcc224a0fb680d3b0a2ddd5e48178deda9`, independently pulled/hashed. Gesture tests in that same invocation still failed due to measured injection overhead exceeding the long-press timeout and a label-leaf selector rather than the actionable parent row. A separate accessibility probe confirmed the same-bounds parent exposes click, long-click and both custom actions; neither failure justifies changing production gesture behavior. Final gesture rerun and normal minified evidence remain pending.


## Final native build and actual 16 KB acceptance

The final native-control/geometry build completed normal minification, formatting and SQLDelight migration verification in 8 minutes 12 seconds. The archive is `r8-instrumentation-abi/native-polygon-final-v11/`: main APK **`a33cb2d39eda6e6c61fb855fba4e66d3571a0303874ba266cb290238ab5b37a3`**, benchmark **`8bc70ea55c8f8c736c640ab7bef64e52a05ca864f33b50019ed4665ca061000d`**, instrumentation **`91191b8206aae9acb408914e922bdfaeae2a0dafcbe4ad006a19b312ea80bb74`**. All 1,262 frozen source files remained identical through compilation and archival. Both application artifacts are non-debuggable, profileable, and pass retained 16 KB alignment checks; those static checks do not replace runtime validation.

On the official **16384-byte** emulator, the exact pulled-installed benchmark/test hashes and signing certificate match the archive. All **20/20 methods pass** in 22.82 seconds: seven actual PNG/SQLite prepared-recovery tests, two image-preparation tests, five durable correction/deletion tests, two portable-history tests and four real native gesture methods. Logical LTR/RTL swipes took 152–215 ms against the actual 400 ms long-press timeout; threshold, stable selection, Back, disabled actions and accessibility action/state assertions pass. Actual TalkBack spoken output remains untested. Restricted read-only inventories found zero owned temporary test roots before and after; the owned inventory probe was then uninstalled. Evidence: `private/native-polygon-20260912/final-device-matrix/emulator16k/`, reconciliation SHA-256 `b8ceeff6e9f6665cd1dd9c4948aec6debe30ecbb773d69c15726048527847375`.

The earlier native gesture failures are preserved: input injection exceeded the long-press timeout and accessibility lookup selected a label leaf instead of its same-bounds actionable parent. The corrected test-only input/lookup passed all four methods on the actual 16 KB debug runtime before the normal minified run. Production gesture behavior was not changed to satisfy that diagnosis.


The K90 has passing outcomes for the same **20 unique methods across two recorded conditions**. Its first invocation passed all 16 geometry/preparation/persistence/archive methods, then HyperOS rejected the gesture activity launch from the background (`Abort background activity starts`, result 102). That invocation retains one launch failure, one interrupted startup and two unstarted methods. A separate foreground invocation on the unchanged APK passed all four gesture methods in 38.64 seconds, with 154–168 ms input durations below the 400 ms long-press timeout. No background-policy change was used to obtain this result. Foreground success does not erase the launch restriction or establish background recovery. Installed main/benchmark/test hashes match the final archive; restricted cache inventories are 0→0. Independent receipt: `private/native-polygon-20260912/final-device-matrix/k90/independent-reconciliation.json`, SHA-256 `6993ff180f3182272d9f845b35367014f6de30247f579a8abedea157052cb440`.

The exact prior emulator `app.mihon.dev.test` APK was restored and independently verified; only the continuation-created `app.mihon.dev` package was removed. Final benchmark/test packages remain installed. No production data reset occurred.

## Matched v10 reader baseline, Ignore SFX on

The v10 classic-reader baseline is now measured under Ignore SFX **on**, separate from older SFX-off fitting comparisons. Installed main APK `55588d60b8fa33d6aa3d02643fd96cb7c0e4ca66138fdf95a7a8ababc1965826` and canonical framework APK `67e851d5c8ae2acfa505262b4c1cc82bc8f96d20f0d4ea1576f24949835820c0` were independently hashed. One calibration pair passed before three alternating pairs on Controlled Translation Validation, chapter `002 - Twelve-page corpus`, page 7. All six acceptance traces cover their own 30-second, 30-gesture windows and match all 3,962 actual frames to unique expected deadlines. Independent reconciliation rehashed 524 artifacts and found zero changed pixels in 15 same-mode/full-width comparisons of ROI `[0,337,1200,2240]`, including the calibration and final screenshot. Original and translated pixels remain distinct; natural drift is retained separately from the verified canonical reset.

| Pair | Translated p95 / late frames | Original p95 / late frames |
|---|---|---|
| 1, translated first | 5.141 ms / 9 | 5.000 ms / 4 |
| 2, original first | 5.212 ms / 6 | 5.218 ms / 12 |
| 3, translated first | 5.631 ms / 13 | 5.104 ms / 14 |

Literal Unknown Jank and Display HAL labels remain visible in the evidence. Original windows 2 and 3 each exceed one expected deadline by 1.367 ms and 0.198 ms respectively; only window 2 carries App Deadline Missed. No dropped frames were recorded. Only translated window 4 has complete scheduler coverage, so these pairs do not establish a CPU-cost difference. Draw-slice wall measurements remain available; allocation and GPU counters are unavailable. The app PID, crash buffer and exit history remained unchanged. This was an AC-charging run (USB-powered flag false), battery 91→93%, battery temperature 30.3→31.3°C; it is not unplugged evidence. Existing exposed source lettering remains visible and is not a passed geometry check.

The phone was returned in translated mode at page 7 with the toolbar visible, high-quality renderer **off**, controlled-series chapter-ahead **0**, automatic translation and automatic quality review **off**, and Ignore SFX **on**, ready for the matched final build. These temporary values still require final restoration. No paid operations ran. Evidence: `build/translator/validation/private/native-controls-20260913-v10-classic-three-pairs-on{,-analysis,-postflight}`, independent receipt `native-controls-20260913-v10-classic-three-pairs-on-independent.json` SHA-256 `b2b54fb1361b48c89d5cdd89c802702b689f9663b1e36333257737b50e2273eb`. Framework run `12386a72-04d3-46f5-b907-54fcbc2c35ea` remains a verified-owned cleanup scope; the calibration and initial zero-input inspection runs remain separate scopes.


## Final classic reader comparison

Final main APK `a33cb2d39eda6e6c61fb855fba4e66d3571a0303874ba266cb290238ab5b37a3` completed one independently accepted classic-reader calibration pair, followed by three alternating original/translated pairs using framework `67e851d5c8ae2acfa505262b4c1cc82bc8f96d20f0d4ea1576f24949835820c0`. All six 30-second windows passed their input, canonical reset, unique expected-deadline and frame-coverage checks. The final six-window receipt rehashed 526 artifacts and verified 14 full-width comparisons at `[0,337,1200,2240]` against the v10 Ignore-SFX-on baseline and restoration; every changed-pixel count was zero. All fourteen corresponding old/new PNGs also matched this ROI. The independent postflight screenshot matched it again. These comparisons preserve the existing source-lettering/orientation failures; they do not establish corrected geometry or human meaning approval.

| Pair order | v10 translated p95 / late rows | Final translated p95 / late rows | v10 original p95 / late rows | Final original p95 / late rows |
|---|---|---|---|---|
| 1: translated, original | 5.141 ms / 9 | 5.381 ms / 11 | 5.000 ms / 4 | 4.859 ms / 10 |
| 2: original, translated | 5.212 ms / 6 | 5.494 ms / 9 | 5.218 ms / 12 | 5.070 ms / 15 |
| 3: translated, original | 5.631 ms / 13 | 5.240 ms / 16 | 5.104 ms / 14 | 4.892 ms / 18 |

The final traces contain 3,963 actual frames, all with unique expected matches, and no App Deadline Missed label, deadline overrun or dropped frame. Literal Unknown Jank and Display HAL late-present labels remain (36 translated and 43 original late rows). Their absence from an app-deadline category is not evidence of universally smooth presentation. Only final original window 2 has complete draw-CPU scheduling; only v10 translated window 4 does, leaving no complete paired CPU comparison. Draw-slice wall data remain available, while allocation rate and GPU render-stage timing are unavailable. These bounded measurements do not isolate a repeatable renderer defect or establish general performance equivalence.

Selected refresh/brightness/rotation/timeout policies match exactly: peak refresh 120, manual brightness 1, timeout 600000 ms and rotation enabled. Both baselines were AC-powered (USB-powered false), but v10 battery/temperature was 91→93% and 30.3→31.3°C, whereas final was 98→100% and 30.9→31.4°C. These are endpoint observations, not continuous thermal monitoring or unplugged results. Final PID 21562, crash-buffer bytes and exit-history bytes remained unchanged. The reader was left at chapter `002 - Twelve-page corpus`, page 7, translated mode with toolbar visible, HQ renderer off, chapter-ahead 0, Ignore SFX on, automatic translation/review off pending the separately recorded final restoration.

Evidence: `private/native-controls-20260913-v11-classic-{calibration-on,three-pairs-on}` and associated `-analysis`, `-pixels.json`, `-independent.json` artifacts. Final calibration receipt SHA-256 `dcca441a787dc321727b2db4d114284c91748479ba24ce31a75e6d3081c651ca`; six-window receipt `b9a58e5d4769cadb917393ebbe55f1654e8d80398a1cd87c257fce706323ed7c`; independent postflight receipt `8b84a376bf9596f6932540daa20b0664002d57fac03a6602c9069b7814544012`. Exact-owned framework cleanup was prepared separately for 23 roots/183 files and requires its actual device receipt before being marked complete. No paid calls or source changes were performed by this measurement/reconciliation work.


## Exact-owned framework cleanup

Exact framework recording cleanup completed before the harness update. The first inspection timed out through the old exec-out stdin transport; its raw failure remains preserved and no deletion was issued in that attempt. Device probes disproved an appended-exit workaround and verified shell -T stdin plus failure-code propagation. A separately pinned runner changed only that script transport, retaining all owner, bounds, hash and file-allowlist checks. Six local transport integration cases passed, and the subsequent real inspection matched all 23 explicitly owned roots / 183 files with no absent or unmatched entries.

Independent review rehashed all 25 inspection and 71 cleanup command outputs, checked every transmitted script against the reviewed exact generator, verified 183 actual REMOVED records and 23 ROOT_REMOVED records, and confirmed every final root was absent. All retained host proofs/files were reverified afterward; old inventories, failed runs and validation evidence remain intact. This closes this allowlist only, excluding unknown historical directories and the two separately inventoried benchmark external preview reports. Inspection receipt SHA-256 `7eace9f0c307693f50a6e7bb6883cb39cd2268b29499a07d7a37931cf0eeb6a4`; cleanup `8502e7353c2caf30169394abbd8c730de8dcfacab0c2e45c530ca013515200e4`; independent reconciliation `b0c95f805d7b843b5dc3d49aadcc268c68ff2dc5b326c630781f09b4b5523f8a`. Evidence is under `private/native-controls-20260913-reader-cleanup-{inventory-v2,shell-v1}/`; the new runner's exact one-line patch and rejected hypothesis are retained separately.

## Actual K90 notification Pause and progress diagnosis

The v11 minified benchmark completed a fresh local-only actual Worker run, `e8075b5c-6c68-4273-b62f-ae86f39b7f70`, using synthetic credentials and the deterministic endpoint. The visible HyperOS chapter card exposed **Pause**, which was tapped through its observed UI bounds. The queue reached PAUSED; a subsequent **programmatic per-job Resume** completed both pages. This is an actual shade Pause plus programmatic Resume result, not shade Resume/Cancel coverage. The first complete result and revision `1789234424191` remain identical and its image was submitted once. Request identities were `0,1,1`; the delayed second response eventually disconnected after 120 seconds and is not counted as client HTTP success. Synthetic usage remains separate from the paid-test ledger.

The five-entry evidence ZIP independently passes manifest, CRC, content hashes, telemetry and first-result preservation checks: SHA-256 `617b5475ea89d6a48c295bbb60e9c381d0700069807c812608eb0e62fd0d693c`. Collection preceded exact-receipt cleanup; the disposable root and telemetry were removed, shared queue events retained, and the test removed only its job and synthetic credential. The owned server and reverse mapping were removed. Benchmark notification permission was restored for user 0 to off, USER_FIXED present and USER_SET absent; other users were not changed. Evidence: `private/native-polygon-20260912/final-notification-shade-k90/` and `k90-final-notification-preflight/restoration.json`.

**Presentation failed independently of recovery:** the expanded card showed `0/2 saved pages` after the durable checkpoint contained one completed page. The v11 run did not record simultaneous posted Android notification extras, so a retained HyperOS modal remains a possible contributor. An exact-source coroutine replay nevertheless reproduced a producer defect in both Worker and notification-center pacing: a sampled final progress update could be consumed by the sampler, rejected by a second one-second gate, and never published without another upstream event. The preserved RED is a deterministic producer reproduction, not retrospective proof of the particular OEM modal's cause.

The focused repair shares a sequential, conflated progress publisher. It retains the latest pending progress, publishes changed states immediately, starts its progress interval after the preceding publication finishes, and does not cancel an in-progress publication. An independent slow-publication regression rejected the first proposed implementation before the final correction. All **eight focused Gradle tests pass**, including last-progress delivery, pending replacement, immediate state changes, finite completion, cancellation, chained publishers and slow publication spacing. Their final XML SHA-256 is `f9fd0d650bb9bc6dc296144c60e5d1cafd71c0bce28e40a9e68c8f9aa5bb3792`.

The actual Worker harness now records its own posted child-card extras and requires the saved 1/2 progress and active stage within 1,500 ms of the first durable-page observation before notification actions. The normal v12 build and fresh runtime checks are pending; v11 observations are not relabelled as v12 passes. No renderer, provider, saved-result, settings or database-schema change was made for this repair. Evidence: `notification-progress-source-repro/` and `notification-progress-gradle-green/` under the same private run directory.


## v12 notification repair build

Normal minification, formatting and migration verification completed in **7 minutes 39 seconds** (603 tasks, 33 executed). Archive `r8-instrumentation-abi/native-polygon-notification-final-v12/` contains main **`e55fcae7e41c6fb32fe8652d5f044c8dcbd2b82bb7a52c9f93409b5c4ac9d31d`**, benchmark **`4c6c3862f06e0518f669522d7effbc4a5369f2d681fea9b16f74ea495956aab5`**, and instrumentation **`08093aac0c99100da5464553583ea0b28f795b7ab1d547e377150eb10166a98a`**. All 1,264 source files remained unchanged through build and archive. Independent rehashing verifies all artifacts, the unchanged signing certificate and 16 KB alignment; application manifests remain non-debuggable and profileable.

Comparison with the v11 source archive identifies exactly five changes: the shared notification publisher, its two consumers, eight host regressions and posted-progress assertions in the actual Worker harness. Renderer, provider, geometry, persisted results and database sources are unchanged. The existing unrelated Glance Compose stack-mapping warning remains nonfatal in both variant logs. Independent build receipt SHA-256 `85e58ed451c7b31587f2816a02bb2bd90d3a5017542bda4fa6c6d7263d565621`; fresh installed/runtime results follow separately. Existing v11 reader measurements retain their original build attribution.


## Independently reconciled v11 preview performance and rendered exports

**PASS within the local test scope.** This independent parser read retained raw instrumentation, reports, every iteration, installed-hash outputs, transfer hashes and observer snapshots. It did not import the runner's analyzer or use its derived pass status as acceptance. It rechecked 598 retained files. No device command, source edit, Gradle invocation or deletion occurred during reconciliation.

Exact measured APKs:

- Benchmark: `8bc70ea55c8f8c736c640ab7bef64e52a05ca864f33b50019ed4665ca061000d`.
- Benchmark Android test: `91191b8206aae9acb408914e922bdfaeae2a0dafcbe4ad006a19b312ea80bb74`.
- Actual K90 runtime: **4096 bytes**; renderer `source-overlay-fit-v5`.

The current five relevant test/renderer sources still match both v11's compile manifest and archived source bytes. This supports retaining these rendering results through a notification-only change; it does **not** make them v12 execution measurements.

### Preview latency and correctness

| Run | Count / elapsed | Preparation time |
|---|---:|---|
| First preparation | 1 | 163.083698ms |
| Three warm calls | 3 | 136.785833, 135.566250, 138.322656ms; mean 136.891580ms |
| Sequential sustained | 1,020 / 180,031ms | Mean 127.937187ms; p50 126.971667ms; p95 134.744063ms; min 125.123490ms; max 148.212031ms |

Sustained throughput was 5.665691 complete preparation/validation/cleanup cycles per second. Its elapsed window includes memory observations and evidence writes. These are one synthetic 1400×1497 page with 20 multilingual/rotated regions, revision 42. “First preparation” does not mean cleared OS, font or graphics caches; no explicit GC or cache flushing occurred.

All 1,024 persisted records are sequential, uniquely identified and complete. Timing sums, iteration totals, compact first-four records, file hashes, runtime and source revision reconcile. Every source stayed unchanged; every preview stayed full-scale 1400×1497, below the 2Mi-pixel ceiling, and every temporary evidence directory was removed. Both invocations report zero provider dispatches, credential accesses and queue jobs. The test checks cache-root removal with both the deletion return and nonexistence; the host did not inspect that private cache independently.

Both invocations have identical hashes:

- Original PNG: `32f4b0283161dffb8285211e99304f88ab367a9b2c192bac231ec6bde9373b6d`.
- Rendered preview PNG: `7b583ad9b5f37b52ea1dc9f27307e21178625ad242f4d36931f33ba54dc668b5`.

The original/preview PNGs were removed by the test; these checks combine runtime hash assertions and cross-record reconciliation. They are not a host rehash of removed images or a human visual/meaning approval.

### Memory, power and temperature

| Runtime boundary measurement | Cold/warm maximum | Sustained maximum | Sustained after owned cleanup |
|---|---:|---:|---:|
| Java used bytes | 15,411,440 | 25,386,648 | 5,319,600 |
| Native allocated bytes | 62,594,080 | 157,770,496 | 44,708,656 |
| Process PSS, KiB | 106,673 | 206,434 | 92,169 |

These are sampled boundaries, not instantaneous peaks, allocation rates or whole-process limits. Java used heap, native allocated heap and PSS are different measurements; do not sum them or infer a leak from an isolated increase.

The separate five-second observer captured **33 matching-PID memory samples** during sustained work, with PSS 67,768–118,657KiB. No host memory sample matched the short cold/warm invocation; that counter remains unavailable there. Android meminfo's partial native-allocation table was available in all 33 matching samples: Bitmap(malloced) 79–85 objects /4,695–53,815KB as labeled, Other(malloced) 520–4,349 /48–414KB, Other(nonmalloced) 92 /74KB. These are category snapshots, not complete allocation counts or allocation rates. GPU performance/memory counters were unavailable; a meminfo Graphics 0 field does not prove zero GPU use.

All recorded boundaries were **Awake, AC-powered (`mPlugType=1`), battery 100%, charging status 2**. USB/Wireless/Dock power flags were false. Peak refresh preference 120, minimum preference unset (`null`), low-power 0 remained unchanged; those settings do not establish actual display refresh during headless work.

- Cold/warm battery service: 30.5→30.5°C; current HAL battery 30.2→30.2°C; skin 29.96184→29.98768°C. Maximum current CPU sensor 34.1→39.3°C.
- Sustained battery service: 30.6→32.7°C; current HAL battery 30.3→32.6°C; skin 30.09560→33.68964°C. Maximum current CPU sensor 35.2→46.3°C at the boundaries, 58.7°C across sampled observations. Framework thermal status remained 0 in all 35 sustained boundary/interval snapshots.

Only **Current temperatures from HAL** were used. The dump also contains old cached hotter values and voltage/current sensor statuses; those are not substituted for current CPU/battery temperatures or device-wide thermal severity. This is a bounded charging run, not unplugged recovery, natural idle, reader pacing or a handset guarantee.

### Rendered exports and cleanup

Exactly the same three methods pass on K90 and the actual 16,384-byte emulator: bounded CBZ Canvas, A4 PDF and Letter PDF through Android PdfRenderer. They verify page order, source-row coverage, original SFX/artwork, masks, unchanged source hash and saved revision. JUnit elapsed was 0.505s on K90 and 0.471s on the emulator; these short times are test execution, not a representative chapter-export benchmark.

K90 instrumentation SHA: `b0bc6ddaee72d438a17997508cdb28194c7ac54166d9200ceed6472ba2d53dcf`.
Emulator instrumentation SHA: `4f9e3b96c714e625be1ee379c1ce1cffd1d1f06d81072dfd438f7e480d9fc767`.

K90's subsequent guarded ExportInventory reports **zero remaining roots** for the two exact UUID cache prefixes; target APK hash matches v11. The independently pulled helper APK matches `7291d9fc9dbe112d46184a03c77296e531a289190893397e958a7d5f30d36f44`; uninstall returned Success and subsequent package path was empty. This is a post-test inventory, not an invented pre-test baseline. The earlier blocked installation attempts remain preserved. No CBZ/PDF file survives these tests for a host visual reinspection, and actual Management/SAF export acceptance remains separate.

### Remaining external files

`owned-external-files-inventory.json` contains **four exact K90 report roots / eight files / 2,709,904 bytes**: the two previously retained SFX performance roots plus the new cold/warm and sustained roots. Each candidate has the exact remote path, host source, size, SHA and historical owner APK. Current presence is unknown; no deletion occurred. The two historical roots belong to an older APK, so a future cleanup must explicitly bind the installed owner/version and recheck each current file. Only matching listed files followed by an empty exact UUID directory are eligible; preserve unknown files and mismatches.

OCR is excluded from that inventory. The K90 final OCR report is retained but has no bound run UUID/remote path in the located receipts. The identifiable persisted `f4228ac1-2867-496f-b0ba-b84d50d2f5e8` OCR report is from the old **emulator-5554** receipt and cannot be treated as K90 data or assumed to exist on the currently running emulator. No broad discovery or wildcard cleanup is proposed.

Machine-readable measurements, hashes, all observer values and exact ownership descriptors are in `summary.json`, `artifact-hashes.json` and `owned-external-files-inventory.json`; reproduction is `python3 -B .../final-local-render-independent-k90/reconcile.py` into a fresh evidence copy because outputs intentionally use exclusive creation.


## v12 runtime gate failures retained

The actual 16384-byte emulator installed and independently verified v12 benchmark/test. Its active-Worker Resume mode passed in 0.498 seconds, reaching completion 156 ms after Resume. The observed first image was ID `1`, and dispatch identities were `1,0,0`; the entire first result and revision `1789235866540` stayed unchanged. This mode does not assert posted-notification freshness.

The subsequent fresh notification-actions run **failed before sending Pause or Resume**. Fifteen posted-card observations retained `0/2 saved pages` and the earlier AI stage, while the durable repository contained one saved page and an active provider-request operation. The same posted timestamp persisted throughout the 1,500 ms observation, so this failure concerns actual Android notification extras rather than only a HyperOS modal. Android logged package notification-rate shedding for summary ID 7101 amid roughly twelve foreground updates within 120 ms; the log does not explicitly name the stale child card. The operation-based immediate-update key remains under investigation. No unqualified retry or cancel-mode run was made, and no failed-run cleanup was asserted. Evidence: `private/native-polygon-20260912/notification-v12-emulator16k/notification-actions/`, run `370d9012-500a-4039-ad5a-143666a2e03d`.

Separately, the v11 WebGPU calibration failed its selected-page boundary assertion after 30 translated gestures. Its original window did not run, so it is not a completed old/new pair. Initial and translated-before page pixels match the old-build baseline, and the failed run's restored screenshot matches its initial full-width ROI. The cause of drift remains under investigation; no renderer change is justified by this observation alone. The original failed report, screenshots and trace remain retained under `private/native-controls-20260913-v11-webgpu-calibration-on/`, run `a7936861-b0ee-4c39-9cf4-a3a688063e71`, report SHA-256 `72ba0e157cbf162c8947cc93c493289170f5ab545dd3b4e03ee9b467167a050e`. Overall acceptance remains open.


## Host storage interruption and evidence preservation

Host storage exhausted during the ten-cycle style recording. The already-open host stdout remained empty, so that transfer failed; it is not a successful command-output receipt. The phone had independently persisted its unique run report and screenshots. The device owner subsequently identified that run by UUID, plan and bounded time window and recovered its durable report and 22 PNGs. The completed ten-cycle device run was not repeated merely to replace missing stdout; its separately verified restoration and readbacks are recorded with that limitation.

To restore headroom, only two reproducible **debug-variant merged/stripped native intermediate directories** were removed (942,639,484 logical bytes), while archived APKs, mappings, original native dependencies and validation evidence remained. Nineteen completed owned classic/WebGPU traces were then transparently compressed using `ditto --noclone --hfsCompression`. All nineteen paths, full logical SHA-256 hashes, byte lengths, ownership, permissions and modification times were rechecked; only compression/storage metadata changed. Space afterward was 2,332,856,320 bytes at that observation. No trace was deleted or renamed. Receipts: `private/native-polygon-20260912/host-reproducible-native-cleanup.json` and `host-trace-compression-v12/receipt.json`.


The new actual-snapshot regression reproduced **13 immediate publications at 0,10,…,120 ms** for twelve routine subprocess transitions. A second regression found routine progress publishing immediately before Pause; the existing retry/error control case already passed. Both RED outputs are retained. The focused key fix groups acquisition/OCR/translation as active work, coalesces routine subprocess/counter changes, and keeps chapter control/terminal states plus actionable operation warnings, retries and failures immediate. Stable identities are sorted for the rate key, so repository ordering alone cannot trigger a burst. Detailed stage/counter contents remain in the published snapshot; no source/result/provider/schema change is involved.

All **13 focused notification methods** now pass: the prior eight publisher tests plus five actual-snapshot/control/reordering methods. New snapshot XML SHA-256 `87906b374e1377a50c4e82079dd71f5c99595d6711e656e8f2af60a8809abce9`; helper XML `b4462a83129838a6b7599bd283ca19fa501a81ed0865ac64e0f7c43dac7fefcf`. Android documents that very frequent updates can be dropped ([notification update guidance](https://developer.android.com/develop/ui/compose/notifications/create-notification)). The retained Android16 NotificationManagerService source also shows package-level update shedding; its rate-limited log alone cannot attribute every dropped child update. Final v13 minification and actual posted-card validation remain pending.


## Native style restoration and fresh controlled baseline

On main v11 with the high-quality reader enabled, one style round trip (19.417 seconds) and ten style cycles (143.988 seconds) passed the recorded UI assertions. Independent analysis rehashed 28 owned files, recomputed 26 PNG ARGB hashes, checked 22 reopened persisted readbacks, and found zero full-width pixel differences after each restoration. The original system font and opacity 0.88 were restored after the alternate monospace/0.35 presentation. The ten-cycle host stdout was lost during ENOSPC; its durable report and exact-owned screenshot archive establish these outcomes without inventing an observed host finish code. Independent receipt `private/native-polygon-20260912/webgpu-style-independent-20260913/summary.json`, SHA-256 `a4f51f4665ba5d9a537ce9a73326319308fee49a50c6c1c770e13b1632db8402`. These results do not approve geometry, meaning or frame pacing.

The normal Management UI exported exactly one controlled history, job `4b133aaa-82c7-4b83-99ea-930f473d9804`, with all twelve pages and both diagnostic checkboxes enabled. Fresh ZIP SHA-256 `68bbd10b1ef36ecd7d4f1d22c90c8fd1b34ea39fbb16ea11874db39b061ffb2c` independently passes CRC, manifest/member hashes and semantic comparisons with the prior preserved baseline: twelve saved results and identities unchanged, zero reviews, no raw OCR present. Current effective Ignore SFX and original-plus-preview review are enabled; the old job's own earlier policy remains unchanged. The historical provider remains service-account JSON Vertex/global/shared/HIGH. The unused Groq URL/dialect fields in that Vertex snapshot do not establish a configured Groq live route. No provider dispatch accompanies this backup. The helper correctly reports pending review coverage, with no structural or sanitized-payload violations (receipt `38620f5d1f44dd845be8e5f79b8a53960fb68782e82b04ff00a2243e83fd023e`).


## WebGPU diagnostic outcome and comparison limits

The preserved v10 high-quality/WebGPU calibration contains one complete original/translated pair on controlled chapter 002, reader page 7, with 1,844/2,160 complete frames. Median frame durations were 3.450/3.447 ms and p95 4.247/4.320 ms; all presented on time with complete expected-deadline matches and no overruns. DrawFrame wall unions were 2,940.659/3,415.590 ms, but incomplete scheduler coverage prevents a draw-CPU comparison; allocation rate and GPU counters remain unavailable. This is bounded calibration evidence, not a handset guarantee. The approved three-pair requirement applies specifically to classic pacing, whose matched results are recorded above.

The first v11 attempt remains failed and unmatched. The separate diagnostic attempt, run 38c87a94-0789-40a9-b239-9e557cd38fa2, uses the same v11 main APK and SDK-only framework 4a81aa66395b58131830555533ddc8d501d47be0bfa8279c106527f529b03739. All timed input, gate and canonical reset code remained byte-identical; only post-window observations were added. Its thirty gestures complete in 30.001215145 s with maximum lateness 17 ms, but the first final boundary is raw/rounded page 6 instead of 7. Both later samples and both PNG scope checks remain 6; the three observations span 561.892 ms. The first failure remains authoritative and the original window is not run.

The failing PNG still shows rotated page 7 passages and the beginning of page 8 signs/SFX, shifted approximately 14 pixels downward. Full-width alignment over 2,100,000 pixels finds 3,346 changed pixels after a 14 px shift, versus 46,140 at zero shift. The archived continuous-reader implementation reports the previous page whenever the viewport top crosses above its current page boundary, consistent with this small natural drift; internal scrollY was not directly measured. This does not reproduce page 6's content replacing page 7, establish a renderer defect, or justify weakening the guard.

Both diagnostic canonical starts and finally restoration match the v10 translated baseline exactly over ROI [0,337,1200,2240]. The diagnostic run remained AC-powered/100%/full (status 5),32.2→32.4°C battery service; v10's translated start was charging (status 2), so power state is not treated as identical. No successful final-build WebGPU pair is claimed. No renderer or gesture correction was applied. Independent receipt: private/native-polygon-20260912/webgpu-boundary-independent-20260913/summary.json, SHA-256 dbff233c0d1cfe7f79491f6dd05670f302590ad7ab1e2947232407a89f2f5617. It rehashes 101 manifest files and five tar members, preserves raw samples and all 129 alignment candidates. The earlier failure remains separate.

## Actual Management CBZ and PDF exports

The native Management UI exported the selected twelve-page controlled history to CBZ and A4 PDF. Independent verification matches all twelve saved source identities, original hashes and dimensions to the controlled corpus. CBZ SHA-256 ed1d986c743159be0da22532b56e913a270a66ce3054e1a9f1e20797f5ff59af (341,829 bytes) contains twelve ordered PNGs and a complete export receipt. PDF SHA-256 dcde02a719ca48f1d368309cd3ebf906677781da662c85b3460edf380b06a73c (452,733 bytes) contains fourteen A4 sheets, 595×842 points.

Every decoded PDF image stream is byte-identical to its corresponding CBZ source rows with verified proportional placement. The 3,600-row strip splits into [0,1698),[1698,3396),[3396,3600), without missing or duplicate rows. The final 204 source rows are white, explaining the blank thirteenth PDF sheet; the final spread follows on sheet 14. The blank source fixture remains pixel-identical.

Ignore SFX works in these actual exports: the entire 420×350 Korean SFX artwork area is unchanged from the source, while both meaningful signs contain changed translated pixels. The saved THUD! translation and region remain in the structured baseline. Poppler inspection confirms the original Korean SFX and English signs. The current sideways-Japanese result still renders Run! horizontally, and source strokes remain visible under opacity 0.88; export fidelity does not close those geometry/meaning gaps. Renderer-version metadata is absent from CBZ/PDF and is supported separately by installed-APK/source evidence. Independent receipt: private/native-polygon-20260912/management-rendered-independent-20260913/summary.json, SHA-256 9897bf149bf6e2bfd69a20a2529b1a450b176a0030985c039f39bc1e1abee5c1. These were cached, local-only exports with no provider dispatch.

## v13 build and installed identity

The normal minified build completed in 7m46s with formatting and SQLDelight migration verification. Archived source freezing reports no changes during execution across 1,265 inputs. Independent signature, manifest and SHA checks verify these immutable artifacts:

| Artifact | SHA-256 |
|---|---|
| Main | `feb6f8a187876487ff210fec52acb46dd592ff1adabe43437b09950449870ea2` |
| Benchmark | `1e634db4d016d07d92809d95dae6752f21117876e4536acc25dda9d11e8fa739` |
| Benchmark tests | `08093aac0c99100da5464553583ea0b28f795b7ab1d547e377150eb10166a98a` |

All three were updated in place on K90, pulled back and rehashed; each matches the archive exactly and the runtime still reports 4,096-byte pages. Data was not cleared. The independent v12→v13 source comparison finds only the notification immediate-update key change and its new five-method snapshot regression; no provider, rendering or schema source changed. The test APK is byte-identical to v12. Prior v11 rendering measurements remain attributed to their actual APK. The fresh 16 KB notification results are reported separately after complete ledger/cleanup reconciliation.

Build evidence: `r8-instrumentation-abi/native-polygon-notification-final-v13/`; independent receipt `private/native-polygon-20260912/v13-build-independent-20260913/summary.json` (SHA-256 `64bf705a6703a5b03f5dae7b47aca62b310eb207c1c07020f3f0f8d98b83c415`); K90 install receipts `install-v13-k90-main`, `install-v13-k90-benchmark`, `install-v13-k90-test` under the same private parent.

## v13 actual 16 KB notification results

Two fresh runs on the official 16,384-byte emulator pass with the exact v13 benchmark/test hashes. The posted chapter card reaches **1/2 saved pages plus the actual provider-request stage** in 956 ms for Pause/Resume and 968 ms for Cancel. The first run completes 2/2 in 1.530 s with dispatch identities `1,0,0`; the first saved image 1/revision `1789237193795` remains identical and is dispatched/saved once. The second cancels in 2.922 s with dispatch identities `0,1`, retaining image 0/revision `1789237348860` and one saved page, with no new dispatch during its bounded post-cancel observation. These use the notification's published PendingIntents under instrumentation, not physical shade taps.

The independent host verifier initially assumed SAVE endedAt equals result revision; actual timestamps disproved that assumption. The corrected invariant is `operation start ≤ revision ≤ operation end`, supplemented by whole-object equality and one SAVE/request. This correction changes no device result. One summary ID 7101 update was rate-shed during rapid Pause/Resume/new-Worker startup after the required chapter-card match. No corresponding child-card loss was observed, but zero summary shedding is not claimed.

Both owned fixture ZIPs were collected, independently reconciled and cleaned. Notifications, flags and the complete channel record match emulator prestate; the owned server/mapping is removed, queue and unfinished Worker counts are zero. The v12 failed root was recollected unchanged under v13 (`44adc00fffe0ece498c84f44871f9554ace691d24727dd817dd8ae31ae9c8caf`) and remains historical evidence. The passing v12 active-Resume measurement is reused only for unchanged Manager/Worker/persistence code.

Evidence: `private/native-polygon-20260912/notification-v13-emulator16k/matrix-reconciliation.json`; Pause/Resume run `94c30ec7-ac79-4b7b-ac2b-b04143ad273f`, ZIP `e920288e737003ac31973542795294b9458cca5b8b7af794d48ce2d5ff74cc7d`; Cancel run `ef36a5c6-7c2f-4d72-9ebd-148a792cb2bb`, ZIP `8103718b11bcd03b94e239afd705c9d82c961641b22ca541cf9a10b0c57d45d6`. All requests used the host-local deterministic endpoint and synthetic credentials; no paid usage was added.

## Controlled v13 original-plus-preview reviews

Exactly two selected-page review actions were made against preserved history `4b133aaa-82c7-4b83-99ea-930f473d9804` after installing v13. Each received a separate **S$5.234689** reservation for maximum documented input/output across both permitted transport attempts. Official Google gross pricing and the ECB 2026-09-11 USD/SGD cross were refreshed immediately before dispatch; promotional credits were not used to reduce the conservative reservation. Service-account JSON, global `gemini-3.8-flash`, shared capacity, historical HIGH thinking, current Ignore SFX and unchanged-original-plus-preview policy were retained. No chapter translation or region retry was clicked.

The warning, image ID4/visibleImage5, completed one transport attempt in the 72.4-second UI run and applied review `b2dd35bd-47d2-48b1-a07a-4bdb42b6f47f`. Its valid saved rectangle now covers **5,395/5,395** source-ink pixels, compared with **3,421/5,395** before. The proposed top is1292.8 versus the authored1296.0; independent rendered artwork/fit checks remain separately required. The complete pre-repair object and Undo baseline are retained. Source and translated text were preserved; detected language and explicitly AI confidence fields changed. The baseline has no raw OCR and continues to store null, so this live run cannot demonstrate preservation of a nonempty OCR payload.

The sideways Japanese, imageID6/visibleImage7, timed out twice. Each generation capture records120,005ms, a fully sent request, zero received response bytes and unavailable HTTP status/usage. The180-second UI recorder timed out while the second attempt was active; an additional read-only observer saw the same review `9b90e0a4-6573-41c8-8532-50f931749fc8` reach **Needs review** at18:30:42UTC. No second UI action or additional pass was made. Its complete saved object/revision and0° physical rotation are unchanged; the required+90° correction remains **failed/unresolved**. Both transport attempts remain financially uncertain. The earlier failure and recorder timeout remain distinct evidence.

Independent ZIP/manifest/capture inspection passes structural checks: all12source identities persist, the ten unaffected pages are exact, the Japanese saved page is exact, both pre-review baselines are exact, and original+preview inputs match across count/generation/retry requests. Original/preview dimensions are1200×1600 with no scaling. No credentials, image bodies or thought signatures were found in sanitized output. Warning count/generation each ran once; Japanese count/generation each ran twice. Incomplete timeout captures are marked incomplete. Actual privately pinned preview PNGs are unavailable after terminal cleanup; their descriptor and capture hashes are retained, not replaced by reconstructed images. AI assessment is not human passage approval.

Successful warning usage is6,312 input tokens (2,128 image subset),775 visible output and9,939 reasoning, estimated at **S$0.113883**. Its reservation was settled once. The entire **S$5.234689** Japanese reservation remains uncertain, alongside the two prior holds. Cumulative known estimate **S$1.245150**, outstanding holds **S$83.696202**, conservative exposure **S$84.941352**; available before theS$270 dispatch stop **S$185.058648**. Billing remains unavailable. No further paid validation is scheduled to obtain a passing result.

Evidence: `private/native-polygon-20260912/live-v13-controlled-reviews/`. BeforeZIP SHA`68bbd10b1ef36ecd7d4f1d22c90c8fd1b34ea39fbb16ea11874db39b061ffb2c`; afterZIP SHA`bdaa183f28336ff48abf5228b241b000876305a50c8acb8ab7553e5f9bf4341c`; independent reconciliation SHA`2d1904bd03c9b328f0bc540169dea53c67d39a3bd084129639734ebfbf38669d`. Framework warning run`1451a3cd-ed3c-4374-83b4-9aba4990b263`; Japanese recorder run`7adc64e0-5b7c-4f30-8dbb-14457f8c5b66`, both on SDK-only framework4a81/mainv13. Successful CBZafter-export SHA`acc5b407d241b56da22de593e4d894dae82603cc623e1f1c92c0d299cf245a29` contains12pages/342,357B, pending the independent pixel findings below.

## Exact owned cleanup before the interior-reader comparison

Three independent host audits and a current device inventory matched **13exact owned UUID roots/86files**, plus the three named earlier SAF exports, to preserved host bytes and installed framework4a81/benchmarkv13/mainv13 hashes. Immediate pre-delete type/size/SHA guards removed exactly those files and empty UUID roots; postinspection reports no retained matching roots or exports. Unknown namespaces, credentials, databases, source chapters, reviews and host evidence were untouched. Cleanup receipt SHA`f33178dd7174e6befc0353d8ce08d1dd8eff14e47595e0da95ca1bd55f0b7f1f`, under `private/native-controls-20260913-final-owned-cleanup-v1/root-cleanup-with-quality/`. A separate pinned cleanup removed the two newly collected review ZIP/CBZ SAF copies only after full host/device hash matching; see `live-v13-controlled-reviews/owned-review-export-cleanup.json`. This does not claim all unrelated historical app files were removed.

## Actual native UI and gesture observations on K90

The v11 main APK (`a33cb2d39eda6e6c61fb855fba4e66d3571a0303874ba266cb290238ab5b37a3`) passed **32 retained UI assertions** against the selected cached controlled chapter. Queue long-press enters selection, a subsequent tap toggles it, Back clears it first, and row swipes remain disabled while selected. A short 50-pixel swipe triggered no action; a completed start swipe on completed work opened the OCR inspector. The end swipe opened the scoped deletion preview showing twelve translation records; it was cancelled without deleting user data.

Log rows independently selected one then two entries and cleared on Back. Their export opens format/options rather than writing immediately. The deletion preview explicitly disclosed its whole-selected-history scope, including events outside the filter, and was cancelled. Saved-history long-press/Back and start-swipe export options worked; cancelling SAF left no destination file. Subsequent uniquely named ZIP/CBZ/PDF exports completed as recorded above. Two sibling subprocess rows selected independently as 1→2→1, preserving the other row when one was deselected; Back cleared both. Related-log navigation exposed the exact controlled job ID. Embedded Management and Dashboard each showed a single parent app bar after clearing selection.

These are actual native screen/dialog/selection observations, not a claim of complete accessibility acceptance. TalkBack was installed but not enabled; spoken flows remain pending. RTL and mixed-active-state acceptance remain separate from these completed-data UI runs; retain their individual instrumentation results and limitations. No retry, paid provider action, saved draft or destructive confirmation occurred during this UI matrix; existing chapter-swipe assignments were retained. Evidence: `private/native-controls-20260913-webgpu-preflight/native-ui-independent-assertions.json`, SHA-256 `20d5fe962987058e07e1cac5c2bf75bcf0e56f5cf0d705b8f57ef384125ebe7d`; all referenced XML hashes were independently rechecked.

## Independent after-review rendered pixels and request window

The after-review CBZ closes the **selected warning export** coverage/fitting check. All twelve original source hashes and dimensions match the corpus. Eleven PNGs are byte-for-byte and RGBA-for-RGBA identical to the earlier CBZ, including the unresolved Japanese page. Only warning image ID4 changes: **5,425 pixels**, all within its validated repaired polygon, with **zero changed pixels outside it** and zero changes in the other three passages. The additional top band lies over white source pixels and introduces no artwork change. The mask covers all **5,395 source-ink pixels**. Independent full-resolution inspection reads the complete, untruncated “KEEP DOOR CLOSED”; its dark glyph bounds `[464,1303,736,1324)` fit within the repaired region. Faint original lettering remains under the configured 0.88 translucent mask. This is shared-renderer export evidence and does not replace reader/backend measurements or imply that every other region is visually correct.

The Korean SFX region remains identical to original source pixels, and its stored “THUD!” translation is preserved. The Japanese export is entirely unchanged, so its required +90° physical correction remains failed. Human passage approval remains pending. Pixel receipt: `live-v13-controlled-reviews/independent-cbz-pixels.json`, SHA-256 `00ffed730cbbb94d2be955fce9a3c4f6fb4b1642ca3e35546af5ae0b7f49273a`; separate visual assessment `8adce52f115abb075a33abcde81702661e1d2b2946631303aa74b4dd0336048e`. Reproduction is the retained `compare_cbz_pixels.py`, which reads exact archived PNGs and extracts unchanged bytes for inspection without image manipulation.

The strongest retained request correlation covers **18:24:23.064–18:30:34.882 UTC on 2026-09-12**, from warning original acquisition through the Japanese durable Needs review event. Its 29 selected-job events include exactly **six request starts: three count-token calls and three review generations**. Every start maps one-to-one to a capture and one of the two selected review IDs; no chapter-translation generation is observed. Generic `translating` events explicitly say “Reviewing saved page 4” and “Reviewing saved page 6”; acquisition and preview-render events are also retained. This describes the exported selected-job window, not all account traffic or unavailable history. Expanded correlation receipt: `independent-request-window-expanded.json`, SHA-256 `fe95ec91ae4d0551c0d9277b48fab723dc54b0567980fcab92d58729016e7b7f`.

## Independent executed-cleanup reconciliation

The final allowlist audit rehashed **22 inspection and 54 cleanup raw command records**, checked every transmitted script against the frozen reviewed generator, and verified **86 exact file removals, 13 empty-root removals and three SAF removals**, followed by explicit absence observations. Every retained host candidate/proof was rehashed afterward. Current owner pins matched framework4a81, benchmarkv13 and mainv13; historical capture attribution remained unchanged. This includes the four previously outstanding performance report roots, so their earlier “current presence unknown” statements describe the pre-cleanup state only. The old failed Japanese recorder report remains preserved on the host.

The two later after-review SAF files were independently matched to their retained host hashes, exact cleanup scripts and root receipt. Their scripts recheck type, byte length and SHA immediately before each exact removal and assert absence afterward, without listing/removing the shared directory. That separate folder does not contain independently archived raw stdout/exit-code artifacts, so its evidence is narrower than the 76 replay-verified command records above. Neither allowlist includes unrelated app namespaces or the unbound OCR report. Independent receipt: `private/native-controls-20260913-final-owned-cleanup-v1/executed-cleanup-independent.json`, SHA-256 `e87c85e1d622ec958178abe69e0c9207e9282414d2c7e26779e15817416acacb`. No device command or deletion was performed by this independent audit.

## Explicit interior WebGPU calibration and final reader restoration

A separate SDK-only protocol was implemented after the retained page-start drift failures: `frameAnchorPixels=100` applies one fixed upward finger drag after every canonical page reset, with 420 ms eased motion and 300 ms release hold. It is restricted to the controlled chapter 002 / reader page 7, preserves the thirty-second gesture loop and first-failure boundary guard byte-for-byte, and requires two identical interior setup hashes before opening any trace gate. All 60 focused host tests passed; framework APK SHA `416cf385ac09f52e3aa53c4928d685ff1d5e491a1456a099b1fcd1665c6f71cf` was built, installed and pulled back for verification. Its archived source/protocol proof is `framework-interior-anchor-20260913/freeze.json`.

The one attempted v10 calibration **failed before any timed window or Perfetto recording**. All three canonical page-start hashes were identical, while the first, repeated and final interior hashes differed. The four retained PNG pixel hashes independently reproduce. Integer alignment over 1,920,000 pixels estimates the first interior at 75 pixels upward and final restoration at 76 pixels upward; this is rendered-image alignment, not direct internal scroll-position measurement. The repeated setup retained only its hash, so its displacement is unavailable. The 100 pixels describe requested finger travel, not guaranteed document displacement. The first failure and incomplete interior-restoration journal remain preserved. No retry, looser guard, production renderer change or v13 timed counterpart followed the failed prerequisite. The required matched WebGPU performance comparison therefore remains pending.

During this bounded failed setup, v10 PID 11796 remained the same, the crash buffer remained byte-identical and thermal status stayed 0. Battery was AC-powered/full 100%, 32.0→32.2°C. There are no timed-window frame, draw-work or allocation measurements to report. This does not broaden the failure into a handset smoothness claim.

Main v13 SHA `feb6f8a187876487ff210fec52acb46dd592ff1adabe43437b09950449870ea2` was restored and verified against the exact on-device APK hash. Reopening chapter 002 / page 7 cleared the offset: the final full-width ROI `[0,337,1200,2240)` matches the v10 canonical-start PNG exactly across **2,283,600 pixels**. Live accessibility range was 7.000000476837158 and the active native view was `ImageViewContinuous`, confirming the high-quality reader remained enabled. The controlled series’ **chapters ahead was restored 0→5**, with autosave and navigation-away/reopen readback proving 5. Both automatic translation and automatic review remain off, Ignore SFX remains on, the queue shows 0 active series/chapters and all 12 saved pages. No style or content edits and no paid calls occurred during this work.

Evidence: `private/native-polygon-20260912/webgpu-interior-matched-k90/`, including `failure-independent.json`, `restoration-independent.json`, `runtime-summary.json` and exact install receipts. Failed framework run `f36aa198-e005-4541-b4bb-681f9a1a3660`, report SHA `b204a7ffb88ef5d88e918eb25b0cc2a63bd89deca22923ef909bbbc2aa23321f`. The new framework 416c ownership inventory contains **three roots/nine files / 672,157 bytes**, fully collected and retained for separately guarded cleanup; this paragraph does not claim those newer roots are already removed. The prior 4a81 cleanup and all older failed calibrations remain unchanged.


## v14 export-dialog accessibility and normal build

The actual v13 Management export dialog exposed two enabled checkboxes without their own accessible labels. A retained UI-tree assertion reproduced both missing-label failures before the fix. The production dialog now gives each entire preference row one labelled checkbox action, a minimum 48 dp touch target and the correct enabled/checked state. The visual Checkbox delegates to that row, following Android’s documented parent `toggleable` / child null-callback pattern and minimum target guidance ([Compose accessibility defaults](https://developer.android.com/develop/ui/compose/accessibility/api-defaults), checked September 13, 2026 SGT). Existing explicit format/export/cancel actions, selected flags, destination handling and busy-state behavior are retained. The dialog extraction is byte-identical outside its former Management block. The acceptance activity is confined to validation variants and never starts an export, repository operation or provider request.

Normal v14 release, benchmark and test builds passed formatting and migration verification in 7 minutes 48 seconds. All 1,268 source files remained frozen through compilation and archival. Exact artifacts are:

| Artifact | SHA-256 |
|---|---|
| Main | `48d5521039ceb552aad9856f992deb14574ab917e971a03cdbc8800f7ebcabe7` |
| Benchmark | `48f4527aa36094f92579f0a834b35e59937b52b740fedd9eecc37cac0e7b9252` |
| Initial test | `32fec7f56b45c70ca44237bb895dc68cd92539d0abe25be3cc1fb2b9427e05be` |

Independent verification checked signatures, exact source additions/changes and the unchanged hashes of all fourteen native libraries against v13. No renderer, provider, database, credential or recovery source changed in this build. Its archive is `r8-instrumentation-abi/native-polygon-accessibility-final-v14/`; independent receipt is `private/native-polygon-20260912/v14-independent-build/receipt.json`.

The first actual 16 KB invocation passed the explicit format-actions method and failed the labelled-row method at an overly specific test assertion: Compose exposes the checkable row as `android.view.View`, not `android.widget.CheckBox`. The retained accessibility observation already has exactly one labelled, checkable, enabled, clickable node per option. The failure is not recorded as a product pass, and downstream assertions in that method had not executed. The test alone was changed to assert the actual checkable-state contract; all label, one-action, checked-state, callback-count, trailing-row touch, disabled, cancellation and export-separation assertions remain. This is a one-line test-source delta, with the original runtime failure preserved. The separately passing format method used actual accessibility actions.

The initial runner's rejection of Android's ordinary two-line installation-success banner is preserved as a host setup failure; it is not an app failure. Postflight confirms unchanged notification flags, app data identity and accessibility services, and no resumed validation activity. Evidence: `export-dialog-v14-emulator16k/{actual-run-1,actual-run-2,postflight-after-actual-failure,test-only-classname-diagnosis}` under the private root. Final corrected-test build and runtime results follow separately.


## Corrected export-contract build and actual runtime results

The follow-up normal build passed in 1 minute 34 seconds (603 tasks: 10 executed, 593 up to date), including formatting and SQLDelight migration verification. Independent comparison finds **exactly one changed test assertion** against the first v14 source archive; all production and validation-activity sources are unchanged. Both main and benchmark APKs are byte-identical to v14. The corrected test APK is **`2cd9d3278a77646743ebe86f32933ea0ce146f0df3b9fd6ef7b80e52c9420c96`** (1,416,273 bytes). All 1,268 frozen source files remained stable during this build. Archive: `r8-instrumentation-abi/native-polygon-accessibility-contract-v14b/`; independent receipt `v14b-independent-build/receipt.json`, SHA-256 `4ff91805b1a8d00b24bd1223785aa92dcace07f6980dcfdbff7009a3a9232603` under the private root.

On the actual **16,384-byte emulator**, both corrected methods pass against unchanged v14 benchmark plus the corrected test APK. This exercises the real production dialog's accessible labels, checkable/checked state, single callbacks, full-row touch target, disabled-state preservation, Cancel without export, and explicit ZIP/PDF/Letter actions through inert validation callbacks. The test APK alone was updated in this rerun. Notification permission/flags, enabled accessibility services, first-install time and data-directory identity remain unchanged; no inert activity remains resumed. The retained v12 failure root was not accessed. This does not constitute spoken TalkBack validation.

The **K90 also passes both methods**, in 16.203 seconds, after in-place installation and independent pulled-hash verification of main, benchmark and corrected test. Its actual runtime remains 4,096 bytes. The same merged accessibility rows report `android.view.View`; all substantive action and state assertions pass. These tests make no provider calls and perform no actual exports or data resets. Evidence: `export-dialog-v14-emulator16k/corrected-actual-run-1/` and `notification-v14-k90/export-dialog-tests/`. Earlier test and host setup failures remain preserved rather than added to unique passing coverage.

The corrected emulator JUnit run took **7.599 seconds**. Independent reconciliation rehashed all 56 outputs from 28 commands and retains the original failures. Receipt SHA-256 `bb031c82fcccd2d379903750c30b476391b8fff2a904aa106e6771eb8ab9d8af`; a separate root postcheck rehashed those outputs and confirmed the actual `16384` observation plus `OK (2 tests)` (`root-postcheck.json`, SHA-256 `d78d996ae516969f7b58f0f88d5edc7196d470094b4cf56bafa80559728841fd`).


## Final K90 published progress and physical Pause

On v14 benchmark with the corrected test APK, one fresh local Worker run passes in **35.563 seconds**. The posted chapter card matches the durable **1/2 saved pages** and **Provider request · 0/1 requests** in **883 ms**, within the 1,500 ms observation bound. The separately captured expanded HyperOS notification displays the same progress/stage. A physical tap uses the current XML bounds of that fixture's Pause button; the durable queue then records PAUSED. A separate programmatic Manager Resume completes 2/2. This proves a physical Pause action and programmatic Resume, not a physical Resume/Cancel tap, natural idle, force-stop recovery or all notification interactions.

The first committed result is image **0**, revision **1789239327045**, identical in all four persisted snapshots and submitted/saved once. Request identities are `0,1,1`: image ID1 (the second page) has an interrupted request followed by its resumed request. The delayed interrupted connection has a separate 120,012 ms terminal server record (`disconnected`); that record was collected before cleanup. Source-ordinal ordering is reported from the actual identities. The test uses only the host-local deterministic endpoint and disposable synthetic credentials, with no paid usage.

Android logs one summary-ID7101 enqueue rate-shedding event immediately after the physical Pause. The initial 1/2/current-stage publication and observed Pause/Resume outcomes pass; zero dropped summary updates is not claimed. This retains the same rapid-control limitation recorded in the v13 emulator run without relabelling its initial-progress regression as unresolved.

Run **`ff69fa65-05be-487e-84a4-5d51878526a0`**, ZIP SHA-256 **`7e746da62b224ddf3ec79d2312cb2bf2eddd9d0f656adaaab62bcad6bbbb7867`**. Independent receipt `notification-v14-k90/shade-pause/independent-reconciliation.json`, SHA-256 `d7d5eaafc75707832038a080fe5dbb96188be36248f20eb33956302db5f047c4`; the separate root postcheck confirms the ZIP, four identical first-result objects and 883 ms publication (`fb3d7e9506e91d1a04c792be9ea8a913516a2396109920a8c6dd3afe02efc6a1`). After collection and verification, qualified cleanup removes only the owned fixture root/telemetry; the test's synthetic credential/job are removed and shared queue events preserved. Cleanup receipt SHA-256 `6ff65554fb99ce5512854a534197a0484a05b7103c8af6060f0fc16a65621434`. Final permission/policy and server/mapping restoration follows separately.


## Final restoration, cleanup and delivery boundary

The K90 retains verified **v14 main `48d5521039ceb552aad9856f992deb14574ab917e971a03cdbc8800f7ebcabe7`**, benchmark `48f4527aa36094f92579f0a834b35e59937b52b740fedd9eecc37cac0e7b9252` and corrected test `2cd9d3278a77646743ebe86f32933ea0ce146f0df3b9fd6ef7b80e52c9420c96`. Updates were in place; no application data was cleared. The actual 16 KB emulator retains that same benchmark/test pair. The main high-quality reader and controlled-series chapter-ahead **5** were independently restored and reopened before the final APK update, and the subsequent validation changed neither preference. Automatic translation/review remain off and Ignore SFX remains on. The main app was reopened to Library after testing.

Benchmark notifications are restored to the exact original user0 line: granted=false, USER_FIXED set, USER_SET absent. The translator notification channel is byte-identical to baseline. Current app-settings UI again shows **Battery saver (recommended), autostart off, notifications No**. Benchmark queue count is zero with no unfinished Worker. The owned local server on8765 and its reverse mapping are absent. Final lifecycle receipt: `notification-v14-k90/final-lifecycle-receipt.json`, SHA-256 `cd60926f2feb306a22d885e0ffae1915a215df75145c9aa08944c6a0471db9b0`. These readbacks do not erase earlier HyperOS screen-off, background-launch, force-stop or notification limitations.

The final SDK416 allowlist was separately reviewed and frozen; fresh device inspection matched all **three roots, nine files, 672,157 bytes**, with no unmatched entries. Exact current framework/main/benchmark hashes were checked before private file access. Each removal rechecked file type, size and SHA, and each root was removed only once empty. The failed calibration's host report/images and historical v10/v13 attribution remain intact. No source chapter, credential, saved result, review, unrelated namespace or shared-storage file was in this allowlist. Independent host checks also passed eleven fake-device tests of the cleanup guards. The separate v12 emulator failure remains retained.

Executed cleanup receipt: `cleanup-sdk416-final-v14/root-cleanup/receipt.json`, SHA-256 `d4dff7bd7cdeda23aaa09daab8a12ac30f438d2b2e59efde6b1ab194e596decd`. Root reconciliation rehashes all inspection/cleanup command outputs, checks nine removals plus three explicit absent-root observations and re-verifies every retained host proof: `root-executed-reconciliation.json`, SHA-256 `a90b67878d5e085e5ad76f9dc313e5ce73471723f6f5fd5cab0261099ee67542`. This adds only these three newer roots to earlier completed, non-overlapping cleanup scopes.

The cumulative spend ledger remains **S$1.245150 estimated usage + S$83.696202 uncertain reservations = S$84.941352 exposure**; billing is unavailable. No paid requests were made by final accessibility, notification, build or cleanup checks. No further live calls were made to seek a passing Japanese result.

**Remaining acceptance gaps:** the controlled sideways Japanese still requires its physical +90° correction; matched WebGPU performance could not pass exact starting-position calibration; spoken TalkBack output and human passage approval are pending. One summary notification update was dropped during the rapid Pause transition. Unavailable GPU/allocation measurements and the power/thermal differences in existing traces remain explicit. Express/OpenAI live validation and cloud billing are conditional on configured credentials/access; local fixture checks do not establish those services. Overall acceptance remains open; neither AI confidence nor successful builds substitute for these missing outcomes.

Delivery update was published and read back exactly on [GitHub issue #1](https://github.com/LegendZ69/mihon/issues/1#issuecomment-5648020543). All 1,268 current compiled-source hashes still match the final archive (`final-source-check.json`); documentation/evidence additions do not change the APK. No commit or push was performed.
