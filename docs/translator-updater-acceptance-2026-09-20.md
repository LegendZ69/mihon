# Updater and translator acceptance — 20 September 2026

Status: **Partially tested; final acceptance remains open.** This checkpoint follows the approved updater and remaining-acceptance plan. Independent agent review is permitted and is identified as such; no human approval is claimed or required by this acceptance process.

## Source and release history

The [historical release report](translator-release-validation-2026-09-20.md) preserves the v16–v19 audit. V16 failed APK signer-output parsing before publication; v18 was deferred after the source advanced. Both are consumed, immutable, unpublished reservations. V15, v17 and v19 assets remain unchanged. Historical device evidence is not reassigned to a newer release.

Upstream `424bbc53b` was merged in `25fdc6669`, retaining the fork-only workflow guards and the upstream runner update. Normal fast-forward pushes advanced the fork mirror and translator branch. Initial updater source is `c337c43ce74ce95b9eb92cc1f81c79ccd553e8e5`, reserved as `translator-v20` by [run 35475429071](https://github.com/LegendZ69/mihon/actions/runs/35475429071). V20 was published successfully. [V21](https://github.com/LegendZ69/mihon/releases/tag/translator-v21), from `ba863df0941edb97960aed393719d6cd6512aa06`, was published by [run 35476064857](https://github.com/LegendZ69/mihon/actions/runs/35476064857). Its public APK exactly matches the locally built release. Runtime validation then found a notification lifecycle defect; these releases and their failures remain immutable evidence.

[V22](https://github.com/LegendZ69/mihon/releases/tag/translator-v22), source `fd38d01c97877f79fa1f2fd4af6374f0f6f45f04` and upstream `424bbc53b85c19acd3c3b7c03ec6f73f516f25bc`, was published by [run 35478264945](https://github.com/LegendZ69/mihon/actions/runs/35478264945). All five public assets match the manifest, checksum list and GitHub digests. All 1,905 source files match the tagged Git blobs, accounting for the declared IDE-file CRLF normalization. The public normal APK is byte-identical to the local release. Normal, benchmark and instrumentation artifacts share the preserved certificate; ARM64 identity and required ZIP/ELF LOAD 16 KB alignment pass. Static RELRO observations are supplementary diagnostics, not an added acceptance requirement.

The old failed hosted run discarded its remote push diagnostic. Its precise server-side cause cannot be recovered from that log. New fixed stage/cause diagnostics distinguish workflow permission, protected ref, non-fast-forward, transport and other failures without publishing remote stderr. A real local Git receive-hook rejection verifies the diagnostics and secret redaction. Future automatic integration of workflow changes may require the dedicated repository-scoped `TRANSLATOR_SYNC_TOKEN` described in [release automation](translator-release-automation.md). No broad workstation OAuth credential was copied to Actions.

## Updater behavior

Numbered production releases discover published fork `translator-vN` releases, including prereleases, by increasing version code. Debug and benchmark packages do not self-update. A successful launch check is throttled for 24 hours; a failed check for one hour; manual checks bypass both. Each version is automatically announced once.

One persistent WorkManager job downloads an explicit user-selected update, retaining progress and retry/cancel state across navigation and process recreation. It verifies manifest identity, byte count, SHA-256, package, version, ABI and signing identity before exposing an APK to Android's installer. Partial bytes and metadata stay outside the updater's FileProvider directory. Download and installation require separate taps; permission denial and installer cancellation retain verified bytes. Relaunch reconciles the installed version and removes obsolete owned files.

## Current evidence

| Gate | Observed result and limit |
|---|---|
| Host Android checks | Immutable v22 CI passed 501 tests across 64 reports, formatting and SQLDelight migrations, with zero failures, errors or skips. Separate local checks passed 516 tests across 68 suites against the unchanged source patch before its commit. All 296 host validation-tool tests passed. V21's 497-test CI result is retained as historical evidence. |
| Release control | The 38 release-control tests passed, including real Git rejection classification, immutable reservations and documentation-only release reuse. They are a subset of the 296 host tool tests. |
| K90 prompt lookup diagnosis | V21 repeatedly failed the System field lookup. Video showed the editor physically present while the harness scrolled past it. V22 waits for editable semantics before scrolling; the unchanged valid-autosave, invalid-draft retention and Back assertions pass in the first full core run on both devices. The historical failures remain recorded; no production semantics change was needed. |
| Runtime identity and native checks | Final v22 benchmark/test hashes match on K90 (4096-byte pages) and the ARM64 emulator (16384). Both first core runs passed 56/56 methods: K90 34.334 seconds, emulator 47.026 seconds. Both additionally passed OCR profiles, interrupted model download and the separate 180-second sustained workload, for 59/59 distinct methods each. The emulator archived 391 SMALL/en OCR iterations over 180.808 seconds, with zero cloud requests and downloads excluded; detailed K90 workload collection remains pending. Plugged local OCR samples do not establish reader, thermal or unplugged performance. Historical v21 emulator gesture failure/isolated pass and K90 54/55 are retained. V21's separate 180-second OCR workloads passed 770 K90 and 376 emulator iterations; overlapping personal K90 work prevents treating those as an isolated performance comparison. |
| Independent passage/visual review | Agent reviewed 12 controlled pages and three retained real pages. Controlled semantics: 25 pass, one fail, one uncertain. Real-page meaning/order checks passed; ten historical regions failed text fit. This is historical-artifact review, not final-build renderer acceptance. |
| Controlled corrections | Derived authored corrections preserve the historical source fixtures and bind exact original hashes/revisions before replay. They address two small top-mask caps, an unstated sister possessor/age nuance, an ambiguous promise subject, and the physical Japanese +90° rotation. Actual final app save/export/Undo remains required. |
| WebGPU comparison | Historical exact calibration failures remain failures. The preliminary v20 slop-primed calibration also failed before any performance trace: canonical page-start pixels matched, but the interior anchor was not repeatable. A final v22 input-timing diagnostic is planned; no new performance result or successful calibration is claimed. |
| Spoken TalkBack | Independent reconciliation of actual preliminary v20 digital audio recognizes More, Translator, Check for updates, New version available and Not now in bounded action-aligned windows. Local ASR interpretation is not human listening, physical gesture or precise speech-latency evidence. Eight helper operations succeeded and four refusals remain: wrong foreground, two duplicate focus-owner cases and a descriptive subtitle mistaken for a paid action. Translator menu focus/speech succeeded; opening that screen was refused. The corrected helper passes 28 host policy checks; final v22 spoken acceptance remains open. Original accessibility services and volume were restored after the preliminary capture. |
| Live providers/accounting | No validation-paid requests or billing queries were dispatched. K90 selected Vertex service account / gemini-3.8-flash; other saved-route access is unconfirmed. UI explicitly reports no billing connection. Additional personal requests and uncertain reservations prevent new paid validation until exposure is reconciled. |
| Updater/install | K90 performed actual v20→v22 through the in-app downloader and vendor installer. Verified bytes survived force-stop and installer cancellation; the retained retry installed the exact public APK. Unknown-source permission was already allowed and remained unchanged. K90 streaming cancellation was not exercised because the download completed quickly. The update row disappeared after relaunch; this is not direct proof of file deletion. Historical emulator v20→v21 separately covers streaming cancellation, duplicate-start rejection, retry, process recreation, permission denial, installer cancellation and obsolete-file removal. |
| Upgrade preservation | Independent comparison of fresh K90 v20/v22 snapshots matches all 11 scoped tables, 31 favorites, 380 jobs and 5,182 results. Sixteen preference XML files and 39 common preference fingerprints match apart from three exact source-known version/updater entries. Dedicated translator settings and one encrypted credential file are byte-identical. Normal application startup was allowed; the probe itself used stable read-only snapshots. This does not prove credential decryptability or live provider authentication. Historical v15→v20 has the same scoped data/vault match but lacks per-entry attribution for its changed common preference file. |
| Notification recovery | V21 real Worker Pause/Resume passed; Pause→Cancel failed before Cancel dispatch because the paused notification disappeared. Android summary cancellation can remove grouped children. V22 separates the foreground card from the center-owned group and observes durable cancellation/removal. Three new notification presentation/lifecycle checks pass in the first full core run on both devices, including actual Android cards across foreground removal/restart. Actual final real-Worker Pause/Resume/Cancel acceptance remains open. |
| Sanitized capture restore | V21 restored saved translations but rejected optional authenticated capture metadata: credential-header omission objects did not fit its typed string map. Two focused regression tests failed before the narrow importer repair and pass afterward, including export/import/re-export with safe header preservation. V22 contains that repair; actual final archive restore remains required. |

The owner made K90 available. Its personal translation queue subsequently stopped naturally: zero active chapters, no queued jobs and no translator foreground service were observed before bootstrap installation. The last dashboard observation showed 232 attempts and USD 222.953472 in outstanding reservations. These are possible exposure, not billed charges, and their overlap with historical validation holds is unconfirmed. No personal queue was paused, resumed or retried by this validation.

## Spending and evidence handling

The existing cumulative ledger remains **S$1.245150 estimated usage + S$83.696202 uncertain holds = S$84.941352 exposure**. The S$270 dispatch stop and S$300 ceiling are unchanged; the ledger arithmetic remainder is S$185.058648, but it is not dispatch permission: additional unreconciled app reservations keep new paid validation on hold. Zero recorded billing means no billing observation, not a zero invoice.

[Official Google pricing](https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing) was checked on September 19 UTC (September 20 locally). Conservative standard pre-credit rates of US$1.50/US$7.50 per million input/output tokens remain the ledger basis. The [ECB reference XML](https://www.ecb.europa.eu/stats/eurofxref/eurofxref-daily.xml) dated September 18 reports USD 1.1460/EUR and SGD 1.4651/EUR; the upward-rounded reference cross-rate is S$1.278446771379/US$. Existing reservations retain their original rates and uncertain holds.

Private logs, complete screenshots, speech and source passages remain under the owned evidence directory. Public reports contain only sanitized outcomes and identities. Initial disk relief removed five allowlisted reproducible native-copy intermediate directories after recording all 278 file hashes; actual free-space increase was **2,498,125,824 bytes**. After v22's first archive attempt ran out of disk, allowlisted regenerated native copies were removed, increasing free space by **2,079,408,128 bytes**; the complete retry archive independently verified. Six duplicate module APK/mapping files were then rehashed against that archive, including decompressed mapping hashes, before removing them and eight regenerable R8 intermediate files; measured free-space increase was **1,286,991,872 bytes**. These are separate event measurements, not cumulative unique net savings. Historical evidence, complete and partial archive sets, native sources/licenses, credentials, saved data and the 16 KB emulator were preserved.

V21 artifact SHA-256 identities (normal release / benchmark / instrumentation):

- `13cf3d9c049c4c156fa8620f864845d2b09a69b4e96285646c01a36671714285`
- `cd43d151cba5a96296b24cb80a383226e9dbb3550b69c47526e8e1323aa1cae3`
- `795c0f0510f2572115b3c71d4791ca8de09a790ab0c62fd75786e220f6fc1fcf`

V22 artifact SHA-256 identities (normal release / benchmark / instrumentation):

- `32c4a3547f3dcc2f6d25765f4dda71b0d65b70997f8fc529a8a00ac5ff200723`
- `444436bacfae12e61388f1bf418d1ffc81b1bf07bd1a8773c9274527f98e04ce`
- `7b817caefb05cb0024330ba60d6a4dcfbedb0cce529dd38bc002678a002c2818`

Both sets use certificate `e6c08810c0687b9471118d71f8a7ef2614d65acb6b8b94a989fbcfa4dc61d1b9`. Each hash set binds only its own release's evidence. V22 remains partially tested while final passage/renderer, spoken TalkBack, real-Worker notification, performance and live-provider/accounting gates remain open.
